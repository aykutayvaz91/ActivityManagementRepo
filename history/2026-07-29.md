# 2026-07-29 — Proje geneli denetim + kritik güvenlik turu (v1.17.1)

## Denetim (4 paralel ajan — proje haritasıyla)
Kullanıcı isteği: "eksik yer / yanlış kurgu / ne geliştirilmeli". 4 boyutta paralel denetim yapıldı: **doğruluk** (talep/entegrasyon), **güvenlik**, **mimari & kurallar**, **kapsam/eksik özellik**. Bulgular tek raporda önceliklendirildi. Özet: mimari sağlam (borç ORTA — yatay kod tekrarı + Tasks modülü tutarsızlıkları), Faz 3 tamamen, Faz 4 büyük oranda bitmiş (CLAUDE.md backlog güncel değil), ama **6 kritik güvenlik açığı** bulundu.

## KRİTİK GÜVENLİK — 6 açık kapatıldı (commit 6cb8674, v1.17.1)
- **K1 Dosya yükleme stored XSS:** `Helpers/UploadValidator.cs` (uzantı beyaz listesi; .html/.svg/.js/.exe reddedilir). Tasks yorum eki + Employee foto + Admin logo'da uygulandı. `Startup.cs` static dosyalarda `X-Content-Type-Options: nosniff` + `/uploads` altında görsel-olmayanlar `Content-Disposition: attachment` → yüklenen içerik tarayıcıda çalışmaz.
- **K2 Talep detayı IDOR:** `ServiceRequestAppService.GetAsync`/`GetEffortsAsync` artık `ApplyVisibilityScope` uyguluyor (listelerle aynı kapsam). Önceden Uzman URL'den herhangi talebin detay/yorum/dosya/eforunu görebiliyordu.
- **K3 Rapor IDOR:** `IReportAppService.GetPersonal/TeamReportAsync` → `[RemoteService(false)]`; kapsam yalnız `ReportsController`'da. Uzman artık `/api/services/app/report/...` ile başkasının raporunu çekemez.
- **K4 Yorum/açıklama stored XSS:** `SafeHtml` regex yerine **Ganss HtmlSanitizer 9.1.973** (beyaz-liste). `<img/src=x/onerror=...>` bypass'ı temizlenir; ekran görüntüsü için yalnız `data:image/*` korunur. (Yeni bağımlılık: HtmlSanitizer + AngleSharp + AngleSharp.Css.)
- **K5 Portal ek indirme SSRF/token sızması:** `DownloadPortalAttachmentAsync` `att.Url`'i https + host allow-list (BaseUrl host + `cortixsuite.azurewebsites.net`) + özel/loopback IP reddiyle doğruluyor; token yalnız doğrulanan host'a.
- **K6 CSRF:** `TasksController` Create/Edit/Delete/UpdateStatus/AddComment/Approve/Reject → `[ValidateAntiForgeryToken]` (Startup'ta global antiforgery filtresi YOK; doğrulandı). Form'lar oto-token, Kanban AJAX token gönderiyor.

**Deploy:** `dotnet publish` ile tam bağımlılık kapanışı → site'a deps.json + HtmlSanitizer/AngleSharp DLL'leri senkronlandı. **Smoke:** admin giriş + yorumlu talep (3277) Detay HTTP 200, "Portal Yorumları" render → HtmlSanitizer runtime'da yükleniyor. AppVersion 1.16.5 → **1.17.1**.

## BEKLEYEN (kullanıcı "diğerlerini sonra bakalım" dedi) — önem sırasıyla
### Orta (doğruluk/perf/borç)
- EmployeeAppService `Delete`/`Update` takım-kapsamı yok (TakımLideri çapraz-takım silebilir).
- Detay senkronu watermark, detay döngüsünden ÖNCE ilerliyor → transient hatada yorum/dosya kalıcı kaybı; watermark'ı detay sonrası ilerlet ya da son-N-gün yeniden dene.
- `PortalStatusText` ingest'te kod/etiket normalize edilmeli (dropdown flip-flop riski).
- Yorum write-back id dönmezse çift yorum riski (cid=null → dedup tutmaz).
- Yetki yardımcıları 12 dosyada kopya (~50x) → `AppServiceBase`'e taşı (en büyük borç).
- `AsNoTracking` tutarsız (Reports/Tasks/ActivityLog/Project okuma uçları); `GetTeamReport` N+1; `ProjectAppService.GetAllAsync` kartezyen Include; Application'da portal HTTP → `IPortalClient`'a çıkar; Tasks `Edit`/`Delete` null-safety/try-catch.
### Düşük (sertleştirme)
- Güvenlik başlıkları (CSP/X-Frame); webhook/admin login sabit-zamanlı karşılaştırma + rate-limit; admin fallback parola `Admin123!` kaldır; sayfa ACL fail-open; AccessControl okuma metotları yetkisiz.
- Ölü kod: `RoutineTask` (şema var, mantık yok), `ParentTaskId`.
### Eksik özellik / geliştirme
- **PDF raporlama** (Faz 4; yalnız Excel var), **Gantt bağımlılıkları** (ParentTaskId), RoutineTask'ı tamamla/kaldır, **global arama**, **toplu (bulk) işlemler**, bildirim tercihleri, kişi kartı efor trendi.
- **CLAUDE.md backlog'u güncelle** (Faz 3/4 bitmiş).

## Entegrasyon (önceki günden devam)
- Destek V3 multipart (yoruma dosya): bizim istemci hazır; destek ucu BOZUK (multipart→400). `destek-entegrasyon-v3-hata-bildirimi.md` + destek yanıtı `destek-entegrasyon-v3-yanit.md` repoda.
- PSM ayrı tasarım bekliyor; write-back durum POST kod/etiket teyidi ilk canlı yazımda.

## Backlog uygulaması (aynı gün, denetim sonrası — v1.17.2 → v1.18.1)
Kullanıcı "yapılacakları yapalım" dedi; batch'ler halinde uygulandı (her batch build+deploy+commit):
- **Batch A (v1.17.2):** Y3 Çözüldü≠Kapatılan (arşiv=yalnız Kapandı+efor); Y1 iç-not portal-reddinde yerel saklama; Y2 faaliyet görünürlük (Admin/Manager/TakımLideri hepsi, Uzman kendi+atanan) + server-side arama. Smoke: /Requests /Activities 200.
- **Batch B (v1.17.3):** EmployeeAppService Delete/Update takım-kapsamı; sync watermark detaydan SONRA + hata sayacı; PortalStatusText kod→etiket normalize; yorum çift-kayıt (push+id-yok → yerel eklenmez).
- **Batch C (v1.17.4):** GetTeamReport N+1 → tek sorgu+GroupBy; ReportAppService/ProjectAppService AsNoTracking; TaskItem.AddComment async I/O; TasksController Edit/Delete null-safety+try/catch. + /Reports (Index view yok → 500) Personal'e redirect.
- **Batch D (v1.17.5):** güvenlik başlıkları (X-Frame/nosniff/Referrer); admin login sabit-zaman + fallback parola "Admin123!" kaldırıldı; webhook X-Api-Key sabit-zaman; AccessControl okuma metotları RemoteService(false); çift HoursSpent kaldırıldı. Smoke: admin login + başlıklar OK.
- **Y5 (v1.18.1):** PSM vs Destek talep detayı AYRI arayüz — /Requests/PsmDetail + PsmDetail.cshtml (sunucu künyesi); Detail↔PsmDetail çapraz-redirect; liste linkleri kaynağa göre. Smoke: PSM PsmDetail 200, destek Detail 200.

**Kalan (yapilacaklar-backlog.md [BEKLİYOR]):** yetki-yardımcısı base-refactor (riskli), Batch E özellikleri (PDF/Gantt-bağımlılık/global-arama/toplu-işlem/bildirim-tercihleri), ACL fail-closed, AsSplitQuery (Relational ref), kalan AsNoTracking sweep, RoutineTask/ParentTaskId ölü şema.

## Destek V3 multipart ÇÖZÜLDÜ (destek tarafı) — dosya yükleme write-back CANLI (v1.18.3)
- Destek `destek-entegrasyon-v3-hata-cozumu.md` ile multipart bug'ını düzeltti (case-insensitive Content-Type + multipart routing). Canlıda doğrulandı: yorum+dosya POST → **HTTP 201** + attachments[] (`sizeBytes`, cortixsuite URL — SSRF allow-list'te). Bizim istemci zaten doğru multipart gönderdiğinden KOD DEĞİŞİKLİĞİ gerekmedi.
- Savunmacı: PortalAttachmentDto artık hem `sizeBytes` hem `size` alanını kabul ediyor (doküman örneği `size`, canlı `sizeBytes`). AppVersion 1.18.3.
- Global arama (Batch E) da tamamlandı (v1.18.2): üst bar, 5 varlık, kapsamlı.

## Tasarım boşluğu kapatma epic'i — TÜM açık noktalar (v1.20.0 → v1.21.0, CANLI)
Kullanıcı "eksik/kurgulanmamış alan" analizi sonrası "tüm açık noktaları kapatalım" dedi. 7 madde 2 batch'te uygulandı (build+deploy+commit+push her batch):

### Batch 1 — v1.20.0 (H1+H2+H4)
- **H1 Devir (handover):** Personel **izne çıkınca / pasife alınınca / silinince** AÇIK görev+talepler 2. sorumluya (yedek) otomatik devredilir; ana↔yedek swap + her devirde **iç not** ile "neden" (İzin/Pasif/Ayrılma + tarih) izlenir. Yedek yoksa iş elde kalır + uyarı notu. `EmployeeAppService.HandoverOpenWorkAsync`; `UpdateAsync` (izin/pasif geçişi, `IsOnLeaveNow`) + `DeleteAsync` tetikler; özet TempData ile bildirilir (`EmployeeDto.HandoverInfo`).
- **H2 Upload → D:\Uploads:** `UploadStorage` yardımcısı (`Storage:UploadsPath`, yoksa wwwroot fallback). Personel foto / görev yorumu eki / marka logosu artık **D:\Uploads** altına. `/uploads` ayrı `PhysicalFileProvider` ile sunulur (nosniff + görsel-olmayan "attachment"). Mevcut dosyalar taşındı, `IIS_IUSRS:(OI)(CI)M` verildi, canlı appsettings.Production.json'a `Storage` merge'lendi (secret yazılmadı). Doğrulama: `/uploads/brand/logo.png` → 200 (D:'den).
- **H4 Yıllık arşiv:** `SystemAuditLogArchive` tablosu (migration `AddSystemAuditLogArchive` — DB'de doğrulandı) + `ArchiveHostedService` (~24s, idempotent, AG-dostu parti commit): bu yılın 1 Ocak'ından ÖNCEKİ denetim kayıtlarını arşive TAŞIR, eski bildirimleri SİLER. Admin **"Denetim Arşivi"** ekranı (`/Admin/SystemLogsArchive` — tarih/yıl aralığı + kullanıcı/işlem/varlık filtresi) + Excel + özet başlık; Sistem Logları'ndan "Arşiv" butonu. `Notification` audit interceptor'dan hariç tutuldu.

### Batch 2 — v1.21.0 (H3+H5+H6+H7)
- **H3 Eşzamanlılık (TaskItem):** Görev düzenleme formunda açılış damgası `OriginalStamp` (LastModificationTime/CreationTime ticks) gizli alanla round-trip; kayıtta DB daha yeni ise **"başkası değiştirdi, yenileyin"** → sessiz üzerine yazma önlenir. Katman ihlali yok (repo üzerinden, DbContext'e dokunmadan). Talepler form-tabanlı düzenlenmediğinden kapsam dışı.
- **H5 Uyarı/eskalasyon:** (a) Talep senkronu **art arda 3 tur** başarısız → tüm aktif **Admin**'lere in-app uyarı (bir kez/seri; başarıda sıfırlanır). (b) **SLA İHLALİ** (son teslim geçmiş, iş açık) → **TAKIM LİDERİNE** eskalasyon (≤3, ≥24s ara). SLA e-posta gönderimi izole try/catch.
- **H6 İş yükü:** Atama dropdown'larında her kişinin **açık görev sayısı** "(N açık iş)" — `GetAllListAsync` tek GroupBy ile `OpenTaskCount` doldurur.
- **H7 Auth denetimi:** admin/Google girişi, **başarısız giriş**, çıkış, **login-as (ActAs)** → dosya audit log + `SystemAuditLogs` (EntityName=Auth). Best-effort (giriş bloklanmaz), uzaktan çağrıya kapalı. **Uçtan uca doğrulandı:** hatalı giriş → `LoginFailed` DB satırı + `[AUTH]` dosya satırı oluştu.

**NOT (rutin işler):** Ayrı "rutin görev" özelliği YAPILMADI — kullanıcı kararı: rutin işler **faaliyet başlığı** (ActivitySubject) altında efor girilerek yönetilir (RoutineTask ölü şeması v1.19.1'de zaten temizlenmişti).

**Durum:** Bekleyen tasarım boşluğu YOK. Kalan uzun-vadeli backlog: yetki-yardımcısı base-refactor (riskli), Batch E (PDF raporlama, Gantt bağımlılık/kritik yol, toplu işlemler, bildirim tercihleri, kişi kartı efor trendi).
