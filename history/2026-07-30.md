# 2026-07-30 — Atanmamış talepler + KALAN BACKLOG'UN TAMAMI (v1.21.1 → v1.22.4)

> Durum sonu: **v1.22.4 canlı**. Uzun vadeli backlog'un TAMAMI bitti (yetki base-refactor + Batch E'nin 5 özelliği). Bilinen bekleyen iş YOK.

## Sürüm şeması notu (ÖNEMLİ)
`ActivityManagementConsts.AppVersion` yorumu: **`1.MAJOR.MINOR`, MINOR .1'den başlar, ".0" KULLANILMAZ**; 5'ten sonra MAJOR atlar, MINOR .1'e döner. Oturum başında yanlışlıkla `.0`'lı sürümler (1.20.0/1.21.0/1.22.0) kullanıldı; 1.22.1'den itibaren şemaya dönüldü (1.22.1→1.22.4).

## Atanmamış Talepler (PSM) + destek kişi filtresi (v1.21.1 → v1.22.0-öncesi)
- **Sorun:** PSM'den sorumlusu atanmadan (AssignedEmployeeId=null) gelen talep, görünürlük kapsamına takılıp Admin/Manager dışında kimseye görünmüyordu (DB'de 1 PSM + 408 destek atanmamış).
- **v1.21.1 (geri alındı):** TÜM atanmamışları herkese açtı → 408 destek ekranı boğdu.
- **v1.21.2 (düzeltme, canlı):** `ApplyVisibilityScope` yalnız **PSM (SunucuKurulum)** atanmamışları herkese gösterir; destek atanmamışı eski kapsamda kalır. "Atanmamış (PSM)" sekmesi (yalnız PSM+açık+sorumsuz). Destek sekmesine **kişi filtresi** dropdown (Tümü / Atanmamış / kişiler — istemci tarafı). `_RequestTable` satırlarına `data-assignee`.

## KALAN BACKLOG — 6 iş, sırayla (her biri build+deploy+commit+push+canlı doğrulama)

### 1) Kişi kartı efor trendi (v1.22.0)
Kişi kartı 360°'a **son 12 ay aylık efor trendi** (bağımlılıksız CSS bar-chart). `EmployeeAppService.GetCardAsync` tek GroupBy ile `EmployeeDto.EffortTrend` doldurur (ActivityLog). Doğrulama: Card/2, Card/4 → 200 + "Efor Trendi".

### 2) PDF raporlama (v1.22.0)
Kişisel + Ekip raporlarında Excel yanına **PDF** export. **PDFsharp-MigraDoc 6.1.1 (core, MIT)** + `PdfFontResolver` (Windows Arial TTF → Türkçe). `ReportPdfBuilder`. Yeni DLL'ler (PdfSharp*/MigraDoc*) + deps.json canlıya kopyalandı. Doğrulama: ExportTeamPdf → geçerli %PDF (70 KB), admin login smoke.
- **NOT (lisans):** QuestPDF Community'nin gelir sınırı olduğundan MIT olan PDFsharp/MigraDoc seçildi. GDI varyantı net8.0'da NU1701 verdiği için core + font resolver kullanıldı.

### 3) Toplu (bulk) işlem — taleplerde toplu atama (v1.22.1)
`ServiceRequestAppService.BulkAssignAsync` (yetki dışı atlanır, Yeni→Atandı + bildirim). `_RequestTable`'a seçim kutusu sütunu (yönetici), Index'e beliren toplu araç çubuğu + "Toplu Ata" modalı (`/Requests/BulkAssign`). Görevler tree/board olduğundan bulk talep tarafında.

### 4) Bildirim tercihleri (v1.22.2)
`NotificationPreference` entity/tablo/migration (EmployeeId, EmailEnabled, MutedTypes). `NotificationManager` susturulan tipi atlar + `IsEmailEnabledAsync` (cache). SLA + görev-atama e-postaları tercihe bağlı. "Bildirim Tercihleri" ekranı (kullanıcı menüsü). Doğrulama: kaydet→DB (EmailEnabled=False, MutedTypes=2,3,4,5,7,9)→temizlik.

### 5) Gantt bağımlılık + kritik yol (v1.22.3)
`TaskDependency` entity/tablo/migration (bitiş→başlangıç). `TaskItemAppService.GetGanttAsync` (görünür görev + bağımlılık csv + **CPM kritik yol** forward/backward pass) + Add/Remove/GetDependencies (döngü koruması, yetki). Gantt view frappe "dependencies" okları + kırmızı kritik bar + açıklama. Görev Detay'da "Bağımlılıklar" bölümü. Doğrulama: bağımlılık ekle → dependsOn + 2 görev kritik → temizle.

### 6) Yetki yardımcıları base-refactor (v1.22.4) — RİSKLİ, dikkatli yapıldı
ServiceRequest/TaskItem/ActivitySubject'teki birebir tekrarlanan yardımcılar `ActivityManagementAppServiceBase`'e taşındı (CurrentContext, IsManager(role), IsCrossTeamManager, IsAdmin(role), IsAdminSelfContext, SeesAllTeams, EffectiveRole, CurrentEmployeeAppRole+cache, CurrentEmployeeTeamId+cache). Base deps'i 3 servis ctor'ında AÇIKÇA atanır (property-injection'a bağımlı değil).
- **Semantik korundu:** Family B (Project/Category/SubCategoryResponsibility — "Manager" hariç IsManager) DOKUNULMADI; SubCatResp.IsManager `new` ile bilinçli gizleme. Ensure*/parametresiz CurrentEmployeeId varyantları da bırakıldı.
- **Doğrulama (canlı, kritik):** admin-self destek=426 (tümü) vs **login-as Uzman#2 destek=5 (kapsamlı)** → SeesAllTeams/EffectiveRole/CurrentEmployeeTeamId login-as dahil doğru. Build 0 uyarı.

## Ortam/araç notu
- `scratchpad\dbq.ps1` bu oturumda yeniden yazıldı (DPAPI LocalMachine ile connection string çözer, ekrana yazmaz). Auth smoke test'lerde admin parolası DPAPI'den çözülüp değişkende kullanıldı, asla yazdırılmadı.
- Deploy: app_offline → DLL kopyala → kaldır → 200 doğrula. PDF için ek olarak PdfSharp*/MigraDoc*.dll + deps.json kopyalandı.

## Bekleyen
**YOK.** Tüm tasarım boşlukları (H1–H7, 2026-07-29) + uzun vadeli backlog (bu gün) tamamlandı.
