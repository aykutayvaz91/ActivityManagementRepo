# 2026-07-25 — Talep (ServiceRequest) Modülü + "İşlerim" Hub + Menü Sadeleştirme (Faz 1)

Önceki gün: `history/2026-07-24.md`. Bu oturumda kullanıcıyla **önce tasarım/brainstorm** yapıldı (AskUserQuestion ile 3+2 karar), sonra Faz 1 kodlanıp canlıya alındı ve GitHub'a push edildi.

## Alınan kararlar (kullanıcı)
1. Entegrasyon: **API/Webhook** (yön "şimdilik netleşmedi" → alıcı endpoint + esnek sync arabirimi hazır bırakıldı, Faz 2'de bağlanacak).
2. Veri modeli: **ayrı `ServiceRequest` entity**.
3. Arayüz: **"İşlerim" hub + üst menüde Talepler** (önerilen).
4. Faz 1'e başla onayı verildi.

## Amaç
İki portaldan gelen talepler eforlu iş olarak yönetilecek:
- **psm.tdv.org** → Sunucu Kurulum talepleri (`RequestSource.SunucuKurulum`)
- **destek.cmit.com.tr** → Dış Destek talepleri (`RequestSource.DisDestek`)

## Yapılanlar (Faz 1)

### Veri modeli
- **Yeni entity `ServiceRequest`** (`Core/Entities/ServiceRequest.cs`): Source, ExternalRef/ExternalUrl (portal no+link), Title/Description, Requester(Name/Email), ExtraInfo (sunucu/müşteri), Status (`RequestStatus`: Yeni/Atandı/DevamEdiyor/Beklemede/Çözüldü/Kapandı/İptal), Priority + PriorityScore(1-10), Assigned/Secondary/Team, Category/SubCategory/Project, ReceivedDate/DueDate/ResolvedDate/ClosedDate, CompletionPercentage, Logs.
- **`ActivityLog.ServiceRequestId`** (+nav) → efor birleşik tabloya yazılır, Günlük Efor otomatik kapsar.
- DbContext: DbSet + konfig (indeks `(Source,ExternalRef)`, FK'ler SetNull/Restrict/NoAction).
- **Migration `V5_ServiceRequests`** (20260725054912) — ServiceRequests tablosu + ActivityLogs.ServiceRequestId. Canlıya açılışta uygulandı (doğrulandı: tablo+kolon var, __EFMigrationsHistory'de kayıtlı).
  - ⚠️ Not: migration üretirken `dotnet ef ... --no-build` eski assembly kullanıp yanlış (IsOnLeave) diff üretti; snapshot git'ten restore edilip **build'li** yeniden üretildi. Ders: migration'ı `--no-build` ile ÜRETME.

### Application
- `ServiceRequests/Dto/ServiceRequestDto.cs` (+ CreateUpdate, GetInput, **PortalRequestDto** Faz 2 için).
- `IServiceRequestAppService` + `ServiceRequestAppService`: GetAll (Source/Status/Assignee/MineOnly/OnlyOpen filtre + **takım izolasyonu + admin-self** deseni), Get, Create/Update/Delete (yönetici), **Assign** (yönetici), **UpdateStatus** (atanan/yönetici; duruma göre ilerleme %), **LogEffort** (yalnız atanan kişi — faaliyet kuralı), GetEfforts, DeleteEffort, **UpsertFromPortalAsync** (idempotent `(Source,ExternalRef)`).
- `ActivityLogDto` + `CreateActivityLogDto`: ServiceRequestId/Title eklendi.
- `AddManualEffortAsync`'e `serviceRequestId` parametresi; `GetDayEffortsAsync` ServiceRequest include + map.

### Web (arayüz)
- **`RequestsController`** + görünümler: `Requests/Index` (2 sekme: Sunucu/Destek + Ata/Durum/Efor modalleri + Yeni Talep), `_RequestTable` (partial), `Requests/Detail` (bilgi + efor listesi + ekle/durum/ata formları).
- **`WorkController` + `Work/Index`** — "İşlerim → Genel Bakış": açık **Görev + Talep + Faaliyet** tek listede, gecikmiş→yakın SLA→yüksek önem sırasıyla, özet kartlar. Personelsiz hesapta (Sistem Yöneticisi) bilgilendirme.
- `WorkItemRow` view model.
- **Günlük Efor**: efor formuna "Talep" hedefi (bana atanan açık talepler); listede "Talep: ..." gösterimi.
- **Menü yeniden düzenlendi** (`_Layout`): **İşlerim** dropdown (Genel Bakış · Görevlerim/Görevler · Pano · Faaliyetler · Görev Sorgula) + **Talepler** dropdown (Tüm/Sunucu/Destek) + Günlük Efor · Projeler · Personeller · Raporlar · Admin. Pano artık İşlerim altında. `#tab-destek/#tab-sunucu` ile sekme otomatik açılır.
- **PageCatalog**: yeni sayfa anahtarları `Work` + `Requests` (3 role de varsayılan erişim; seed açılışta eksik satırları ekler).

### Faz 2 iskeleti (entegrasyon — şu an KAPALI)
- **`IntegrationController`** (`POST /api/integration/requests`, `GET /api/integration/ping`): `X-Api-Key` header ile doğrulama; `Integration:ApiKey` appsettings'te **tanımlı değilse 503** (kapalı). `UpsertFromPortalAsync` ile idempotent upsert.
  - ⚠️ Not: ilk sürüm düz `ControllerBase`'ten türeyince Windsor DI'ına kaydolmadı (500). Tüm controller'lar gibi `ActivityManagementControllerBase` yapıldı → düzeldi.
  - Yanıt şu an ABP zarfıyla (`{result:{...},success:true}`) dönüyor; gerçek portal bağlanırken sözleşme kesinleştirilecek (gerekirse DontWrapResult).
- **`IRequestSyncClient`** arabirimi (PULL alternatifi için sözleşme; çalışan uyarlama/HostedService yok).

## Doğrulama (canlı)
- Build 0/0. Deploy: Core+Application+EntityFrameworkCore+Web.Mvc DLL. Site 302 OK. Bugün (2026-07-25) error log **temiz** (son hata 24'ten, ilgisiz Google OAuth timeout).
- Sayfalar: `/Requests` 200, `/Work` 200, `/Activities/Today` 200, `/api/integration/ping` 200 `{ok:true,enabled:false}`.
- Uçtan uca: talep **oluştur → listele → sil** başarılı (Atandı, atanan=Aykut, takım=Infrastructure Team; sonra temizlendi).
- Yedek: `scratchpad/backup2/*_talepler`.
- GitHub: commit + push (token maskeli).

## Nasıl kullanılır (özet)
- **Talepler** menüsü → Sunucu/Destek sekmesi → Yeni Talep (yönetici) / Ata / Durum / Efor. Detayda tam bilgi + efor geçmişi.
- **İşlerim → Genel Bakış**: üzerindeki tüm açık işler tek ekranda (SLA/önem sırası).
- **Günlük Efor**: "Talep" seçilerek talebe harcanan süre girilir.

---

# FAZ 2 — Entegrasyon altyapısı (kendi tarafımız) — AYNI GÜN tamamlandı

Kullanıcı: "kendi tarafımızı yazalım hazır olsun" + "konfig ayarlarını admin yapabilmeli, arayüzü yap". Ayrıca iki portal ekran görüntüsünden alan analizi yapılıp **iki hand-off dokümanı** üretildi: `destek-entegrasyon.md`, `psm-entegrasyon.md`.

## Yapılanlar
- **Ayar entity'leri (DB, admin yönetir):** `IntegrationSettings` (singleton: InboundApiKey, SyncEnabled, IntervalMinutes) + `IntegrationSource` (kaynak başına: Enabled, BaseUrl, ApiKey, AuthHeader/AuthScheme, Filter, InitialLookbackDays, LastSyncUtc/LastRunUtc/LastResult). **Migration `V6_Integration`** (canlıda uygulandı). Seed: Sunucu+Destek 2 satır (kapalı) + settings satırı (idempotent).
- **`IIntegrationSettingsAppService`**: GetAsync/SaveGeneralAsync/SaveSourceAsync (admin), `GetInboundAsync` (anonim webhook için), anahtarlar EmailSettings deseninde (boş=değişmez; ayrıca "temizle" seçeneği → webhook kapatılır).
- **Admin ekranı** `/Admin/Integration` (+ Admin panel kartı): Gelen(webhook) anahtarı + webhook adresi gösterimi + master pull switch + aralık; her kaynak için (Sunucu/Destek) Enabled/BaseUrl/ApiKey/Auth/Filter/lookback + **son çalışma/watermark/sonuç** göstergesi.
- **Webhook alıcısı** (`IntegrationController`) artık inbound anahtarı **DB'den** okur (appsettings değil); yoksa 503.
- **`UpsertFromPortalAsync` zenginleştirildi:** ham alan çözümleme — `assigneeEmail`→Employee, `group`→Team, `statusText`→RequestStatus (TR/EN varyant eşleme), `priorityText`→PriorityScore. Atama/durum yerelde korunur; portal yalnız kapanış/iptal bildirince kapatır. İlk içe aktarımda portal ataması uygulanır.
- **`RequestSyncHostedService`** (PULL): DB'den aktif kaynakları okur, portal okuma ucundan JSON çeker (`{items:[...]}` zarfı veya düz dizi), `WireItem`→`PortalRequestDto` map (psm `requested/installed/services` → ExtraInfo düz metin, **parola yok**), `UpsertFromPortalAsync` ile idempotent, watermark ilerletir. **Varsayılan KAPALI** (SyncEnabled=false → no-op). Startup'ta `AddHttpClient` + `AddHostedService`.

## Doğrulama (canlı)
- Build 0/0. Deploy 4 katman + (2. tur) Application+Web.Mvc. Site 302, bugün entegrasyon hatası yok.
- `/Admin/Integration` 200; anahtar kaydet→ping enabled:true; **anahtar temizle**→enabled:false.
- **Webhook uçtan uca:** UTF-8 gövdeyle POST → **200**, kayıt oluştu; **iki POST → tek kayıt (idempotent)**; `statusText:"Açık"`→**Yeni**, `priorityText:"Yüksek"`→**7**; Türkçe karakter korundu; test kaydı silindi; anahtar temizlendi (**prod temiz**).
- Not: ilk denemede PowerShell Türkçe `ç`'yi Windows-1254 tek bayt (0xE7) yollayınca sunucu doğru şekilde 500/decoder hatası verdi → UTF-8 bayt ile çözüldü (kod sağlam).
- Yedek: `scratchpad/backup2/*_integration`.

## Kalan (portal ekiplerinden gelince)
Config admin panelinden girilecek. Portal ekiplerine `destek-entegrasyon.md` / `psm-entegrasyon.md` iletilecek; dönüş (API var mı / webhook mı, BaseUrl+auth, durum listesi, e-posta alanı, ağ erişimi) sonrası: **webhook** ise portallar `/api/integration/requests`'e POST atar (admin anahtarı verir); **pull** ise admin ekranından BaseUrl+ApiKey+Filter girilip kaynak "etkin" yapılır. Gerekirse portal yanıt şemasına göre `WireItem`/mapping ince ayarı.

---

# BİLDİRİM (Notification) — Polling — AYNI GÜN

Kullanıcı brainstorm sonrası seçti: **mekanizma = Polling**, **olaylar = Görev/Talep atandı + SLA yaklaştı**. (Ayrıca iyileştirme partileri planlandı; bu ilk sırada yapıldı.)

## Yapılanlar
- **`Notification` entity** (RecipientEmployeeId, Type, Title, Message, Link, Icon, Severity, IsRead) + **migration V7** (canlıda). ⚠️ DbSet adı `AppNotifications` (base `AbpZeroDbContext.Notifications`'ı gizlememek için; tablo adı "Notifications").
- **`INotificationManager`/`NotificationManager`** — bildirim OLUŞTURMA; `IApplicationService` DEĞİL → dış API'ye açılmaz (spam engeli). `NotifyAsync(recipient, type, title, msg, link, icon, severity, actorEmployeeId)`; kendi işlemine bildirim gitmez (actor==recipient atlanır).
- **`INotificationAppService`/`NotificationAppService`** — okuma/işaretleme: `GetSummaryAsync` (okunmamış sayısı + son 15), `MarkReadAsync`, `MarkAllReadAsync`; hepsi geçerli `EmployeeId`'ye kısıtlı (login-as uyumlu).
- **`NotificationsController`** (MVC): `GET Summary` (JSON), `POST MarkRead/MarkAllRead` (antiforgery) — dynamic API antiforgery belirsizliğine girmeden.
- **Tetikleyiciler:** Görev atandı (`TaskItem.Create` + `Update` yeniden atama), Talep atandı (`ServiceRequest.Assign` + `Create`), SLA yaklaştı (`SlaReminderHostedService` — görev + **talep** için yarın SLA'sı olanlara in-app; mevcut e-postaya ek).
- **UI (_Layout):** navbar'da 🔔 + okunmamış rozeti + açılır liste (`__hasEmp` olan kullanıcıda). ~25 sn polling; yeni gelince **yandan `abp.notify` toast + Web Audio "ding"** (autoplay: ilk tıklamadan sonra ses açılır). Yanıt ABP zarfı (`__abp`) açılır. Tıkla → okundu + ilgili kayda git.

## Doğrulama (canlı, uçtan uca)
- Build 0/0, deploy 4 katman, V7 uygulandı, Notifications tablosu var, hata yok.
- Admin Aykut(2)'ye talep oluşturdu → **bildirim oluştu**; Aykut olarak `Summary` okunmamış=1, "Size bir talep atandı" + link=/Requests/Detail/3; **MarkAllRead → 0**; test talebi silindi.
- Yedek: `scratchpad/backup2/*_notif`. (Not: Aykut'ta 1 okunmuş test bildirimi kaldı — read-only DB ile silinemedi; zararsız.)

## Bekleyen iyileştirme partileri (kullanıcı onayladı, sırada)
- **Parti 1:** sırları şifrele (SMTP/entegrasyon anahtarları DPAPI) + talep eforu raporlara + `TaskItem.ActualHours` senkron.
- **Parti 3:** panoya talepler + sürükle-bırak durum.
- **Parti 4:** dynamic API yetki denetimi (AppService seviyesi).
- (Bildirim N2 ops.: SignalR anlık push, kullanıcı ses/tip tercihleri, durum-değişti & yorum tetikleyicileri.)

---

# İYİLEŞTİRME PARTİLERİ (Parti 1/3/4) + SİSTEM DOKÜMANI — AYNI GÜN

Kullanıcı: "eksik hiçbir iş bırakma" + "tüm sistemi gözden geçir bug/geliştirme, sonra sistem dokümanı". Parti sırası kullanıcı isteğiyle: bildirim (yukarıda) → sonra 1/3/4.

## Parti 1 — Güvenlik/tutarlılık (commit 0cad597)
- **Sırlar DPAPI ile şifreli:** `EmailSettings.SmtpPassword`, `IntegrationSettings.InboundApiKey`, `IntegrationSource.ApiKey` → yazınca `DpapiProtector.Protect`, kullanınca `Unprotect`. Açılışta legacy düz-metin göçü (`SeedDataBuilder.EncryptLegacySecrets`).
  - ⚠️ BUG bulundu+düzeltildi: şifreli değer ~314 krkt; kolonlar 256/512 taşıyordu → **migration V8** ile 1024. (Test sırasında "String or binary data would be truncated" ile yakalandı.)
- **Talep eforu raporlara:** `ReportAppService` sorgusuna `ServiceRequest` include + DetailedActivities'te "Talep: …" etiketi (efor zaten ActivityLog'tan "Talep" tipiyle sayılıyordu).
- **`TaskItem.ActualHours` senkronu:** görev eforu eklenince/düzenlenince/silinince ActualHours = ActivityLog toplamı (`ActivitySubjectAppService` + `ActivityLogAppService`).

## Parti 4 — API yetki denetimi (commit 0cad597)
Denetim ajanı dynamic API açıklarını buldu; kapatıldı:
- **YÜKSEK:** `Employee.UpdateRole` (→EnsureAdmin), `WorkflowStatus.Create/Update/Delete` (→EnsureAdmin), `ServiceRequest.UpsertFromPortal` (kimliği doğrulanmış non-admin'e guard).
- **ORTA:** `Employee.Update` hassas alanlar (rol/aktiflik/hesap/takım) yalnız Admin → **rol yükseltme kapandı**; `Employee.Create` non-admin→"Uzman" sabit; `IntegrationSettings.GetInbound` non-admin'e guard.
- **DÜŞÜK:** efor düzenle/sil (`ActivitySubject.UpdateEffort`, `ActivityLog.Create/Delete`) TakımLideri yalnız kendi takımı.
- Not: `[RemoteService(false)]` bu ABP sürümünde uçları gizlemedi (415/200 döndü) → çalışma-zamanı guard kullanıldı (anonim webhook + HostedService + Admin geçer, diğer authenticated reddedilir). Doğrulandı: anonim webhook 200, InboundApiKey DB'de şifreli (314 krkt).

## Parti 3 — Panoya talepler + sürükle-bırak (commit e49f9da)
- Pano'da **mod seçici** (İkisi/Görevler/Talepler). Talepler `reqToCol` ile kolonlara (mavi kenarlık + Talep ikonu). Talep kartı sürüklenince `colToReq` ile RequestStatus'a eşlenip `/Requests/UpdateStatus`. Yalnız kendine ait (canManage/canLogEffort) kartlar sürüklenir.

## Sistem Dokümanı
- **`SISTEM.md`** oluşturuldu: genel bakış, mimari/ortam, kimlik+yetki (login-as, admin-self, dynamic API güvenliği), veri modeli, görünürlük/işlem kuralı, modüller/menü, efor+ilerleme%, bildirim, entegrasyon, güvenlik, hata yönetimi, deploy, kod/klasör rehberi, yol haritası.

## Doğrulama (canlı)
- Migration V6/V7/V8 uygulandı. Build 0/0. Site 302. Deploy'lar backup2'ye yedekli.
- Webhook uçtan uca 200 (anonim), sır şifreleme (314 krkt), bildirim uçtan uca, pano 200 (mod seçici) — hepsi doğrulandı. Bugün beklenmeyen hata yok.

## Diğer bekleyen
- Bildirim N2 (SignalR/tercihler/ek tetikleyiciler) · Faz 3 (görev onay + rich text yorum) · Faz 4 (kişi kartı 360°, Gantt, Excel/PDF raporlama) — CLAUDE.md.

---

## Hiyerarşi kısıtları (commit daad986, v1.11.3)
- **Sistem Yöneticisi (IsSystemAccount) dropdown dışı:** `EmployeeAppService.GetAllListAsync/GetAllAsync` → `!e.IsSystemAccount`; Personeller listesi/sayımı + görev 1./2. sorumlu seçeneklerinden çıktı.
- **Üste görev atanamaz:** `TaskItemAppService.EnsureCanAssignAsync` — atanan/2. sorumlunun `RoleRank`'i atayandan yüksekse `UserFriendlyException` ("Hiyerarşik üstünüze (X) görev atayamazsınız…"). Create + Update'te çağrılır. RoleRank: Uzman(1) < TakımLideri(2) < Manager(3) < Admin(4).

## İstek/Mesaj kutusu — zil yanından üste istek gönderme (commit fbc31f1, v1.11.4)
Kullanıcı isteği: "isteği şu bildirim yanına mesaj kutusu olarak eklesen ordan iletse şık olur … ordan admine yöneticiye takım liderine ulaşabilir".
- **UI (`_Layout.cshtml`):** zil ikonu yanına `#msgBtn` paper-plane dropdown (data-bs-auto-close="outside"): `#msgTo` alıcı select + `#msgText` textarea + Gönder. İlk açılışta `loadRecipients()` lazy yükler; `sendUpMessage()` POST atar. `esc`/`afToken` mevcut IIFE'den yeniden kullanıldı.
- **`NotificationType.Mesaj = 7`** (`Notification.cs`); gönderenin kartına link (`/Employees/Card/{myId}`), `fa-paper-plane` ikon, `info` severity.
- **`NotificationAppService`** (`_empRepo` enjekte edildi → ctor değişti):
  - `GetMessageRecipientsAsync` → yalnız **üst rütbeli** (RoleRank > CurrentRole) aktif personel; Sistem Yön./Admin dahil ("(Yönetim)" etiketiyle). `MessageRecipientDto {Id, Name, RoleLabel}`.
  - `SendMessageAsync(recipientEmployeeId, message)` → gönderen DB `me.AppRole` ile değerlendirilir; `RoleRank(to) <= RoleRank(me)` ise guard; değilse alıcının ziline `Notification` düşer.
- **`NotificationsController`:** `Recipients` (GET→Json) + `SendMessage` (POST, antiforgery; UFE→400 text/plain, hata→500+ErrorLog).
- **Doğrulama (canlı):** build 0/0, deploy v1.11.4, site 302. GET Recipients JSON döndü. **Mutlu yol pozitif test**: admin→ActAs emp#2 (Uzman, EmployeeId claim=2) → SendMessage→emp#1 (TakımLideri) = HTTP 200; DB'de Type=7 kayıt oluştu (Title/Link/Icon doğru) → **test kaydı silindi** (canlı kirlenmedi). Canlı HTML'de msgBtn/msgTo/msgText/sendUpMessage/loadRecipients mevcut, footer v1.11.4.
- **Not (küçük/kozmetik):** UFE guard reddinde `ex.Message` controller'a boş ulaşıyor (ABP AppService proxy'si) → JS `t || 'Gönderilemedi.'` fallback'i devrede. Dropdown önceden filtreli olduğundan guard gerçek akışta neredeyse hiç tetiklenmez; success/UI mesajları hardcoded ("İsteğiniz iletildi."). GetMessageRecipients rol **claim**'ini, SendMessage DB **AppRole**'ünü kullanır — gerçek (ActAs'sız) kullanıcıda ikisi aynı, tutarlı.

## Manager rolü takımsız kalır (commit 5a20360, v1.11.5)
Sorun (kullanıcı): "Recep Şahin Infrastructure team'de değil nasıl oraya aldın çıkart, takımsız olacak ama her takımın şeylerini görebilecek admin gibi".
- **Kök neden:** `SeedDataBuilder.SeedTeam` her açılışta **takımsız tüm personeli** varsayılan takıma (Infrastructure) bağlıyordu. Recep UI'dan Manager olarak eklenmiş; seed onu team 1'e çekmiş. (Recep seed'de yok; Id 8, UI'dan.)
- **Çözüm — 4 nokta:**
  - `SeedDataBuilder.SeedTeam`: Manager rolü de (sistem admin gibi) takıma bağlanmaz; yanlışlıkla bağlıysa `TeamId=null`'a onarılır (idempotent, açılışta).
  - `EmployeeAppService.CreateAsync/UpdateAsync/UpdateRoleAsync`: rol **Manager** ise `TeamId=null` zorlanır (UI/DTO'dan gelse bile).
  - `Employees/Create.cshtml` + `Edit.cshtml`: rol "Manager" seçilince **Takım select devre dışı** + "Manager tüm takımları görür — takıma bağlanmaz" notu (JS `sync()`).
- **Neden görünürlük bozulmuyor:** Manager görünürlüğü **role dayalı**, TeamId'den bağımsız (`TeamScopeAsync` Manager→`(false,null)` = filtre yok). Takımsız Manager yine tüm takımları görür.
- **Doğrulama (canlı):** deploy sonrası seed Recep'i `TeamId=null`'a çekti (Sistem Yön. de takımsız). Recep(Manager) olarak /Employees /Tasks /Projects / = 200; GetAllList = **7 personel** (tümü). Build 0/0, site 302, v1.11.5.

## Takımsız kullanıcıda üst marka "FYS" (commit f32eeb4, v1.12.1)
Kullanıcı: "Takımsızlarda da üstte FYS yazsın, Faaliyet Yönetim Sistemi uzun."
- **Sorun:** takım-marka modu (`UseTeamNameAsBrand`) açıkken takımsız kullanıcı (ör. Manager Recep) `CurrentTeamShortNameAsync`→null döndüğü için uzun `BrandName`'e ("Faaliyet Yönetim Sistemi") düşüyordu.
- **Çözüm:** `ThemeSettingsAppService.CurrentTeamShortNameAsync` — `!TeamId.HasValue` → **"FYS"**; takım kısa adı boşsa da "FYS". (Sistem Yön.→"ADMIN", takımlı→"INFRA" aynen.)
- **Doğrulama (canlı):** üst-bar markası — admin-self=**ADMIN**, Recep(Manager,takımsız)=**FYS**, Aykut(Infrastructure)=**INFRA**. Build 0/0, site 302, v1.12.1.
- Sürüm şeması: 1.11.5 → **1.12.1** (.5 sonrası MAJOR atla, MINOR .1'e döner).

## Bildirim: tıklayınca anında okundu + rozet düşsün (commit da0b21e, v1.12.2)
Kullanıcı: "şu bildirimlere basınca okundu olsun üstteki kırmızı azalsın okunmayan olarak göstersin".
- **Sorun:** link'li bildirime tıklayınca sayfa yönleniyor; rozet (üstteki kırmızı) yalnız 25 sn'lik polling'de yenileniyordu → anlık geri bildirim yoktu. **Sunucu tarafı MarkRead/MarkAllRead zaten doğru** (test: unread 5→4, HTTP 200) — eksik olan istemci UX'iydi.
- **Çözüm (`_Layout.cshtml`, salt istemci):**
  - `notifClick(id, link, wasRead)`: okunmamışsa **optimistik** `decBadge()` (rozeti bir azalt, 0'da gizle) + satırı okundu göster (mavi arka plan + `.notif-dot` kaldır), sonra `MarkRead` POST, sonra link'e git / `load(false)`. `.catch` ile ağ hatasında da yönlenir.
  - `markAllNotifRead`: rozet anında gizlenir + tüm satırlar okundu gösterilir.
  - `render`: satıra `id="notif-{id}"`, noktaya `notif-dot` sınıfı, `onclick`'e `wasRead` parametresi.
- **Doğrulama (canlı):** decBadge + `notifClick(...,wasRead)` imzası canlı HTML'de mevcut; MarkRead unread 5→4. Build 0/0, site 302, v1.12.2.

## Görev otomatik arşiv: "Kapatıldı" + %'lerde sayılmama (commit b1e7cf2, v1.12.3)
Kullanıcı: "tamamlandı görevleri 1 hafta sonra kapatıldı yapsın otomatik %lerde hesaplanmasın" → mid-turn düzeltme: "**aylık raporda çekiyoruz ay geçtikten sonra** kapatıldı yapalım, haftalık mantıksız olabilir".
- **Yeni durum `TaskStatus.Kapatildi = 5`** (arşiv). Migration YOK (int kolon; 5 yeni geçerli değer).
- **Otomatik kapatma — `TaskAutoCloseHostedService`** (Startup'ta kayıtlı, ~12 saatte bir, açılışta 4 dk gecikme): `Status=Tamamlandı` ve `CompletedDate < içinde bulunulan ayın 1'i` olan görevler → `Kapatildi`. **CompletedDate KORUNUR** (rapor için şart).
- **Aktif % (Kapatıldı hariç):** `ProjectAppService.MapToProjectDto` → `TaskCount = Count(Status != Kapatildi)` (paydadan düşer), completed=Tamamlandı. Home dashboard (`TotalTaskCount`/sayaçlar/`RecentTasks`), `WorkController` açık-iş, `ActivitiesController` aktif görev, `MyTasks` listesi Kapatıldı'yı hariç tutar. **Pano**: durum kolonu (0-4) eşleşmediğinden Kapatıldı görev zaten görünmez (arşiv) — ekstra kod yok.
- **Geçmiş/tarih-kapsamlı raporlar (bozulmaz):** `ReportAppService`'te 5 yerde `Status==Tamamlandi` → `(Tamamlandi || Kapatildi)` — kapatılan görev geçmiş aylık raporda hâlâ "tamamlanmış iş". (CompletedDate korunduğu için tarih filtresi de tutar.)
- **Etiket/renk:** `TaskItemAppService.StatusText` merkezî Türkçe ("Kapatıldı"); `ProgressForStatus` Kapatildi→100; CompletedDate koruması Update/UpdateStatus'ta Kapatildi'yi de kapsar. View dizileri: Card/Completed/Detail/Index/MyTasks (index 5 = "Kapatıldı", renk secondary). SLA servisi kapatılmış göreve hatırlatma göndermez.
- **Doğrulama (canlı):** proje 5'e geçen ay (2026-06-15) tamamlanmış test görevi eklendi → job ~5 dk'da `Tamamlandı(2)→Kapatıldı(5)`; proje sayacı **2/1 → 1/0** (%'den düştü); **CompletedDate korundu**; test görev silindi. Gerçekte kapatılacak geçmiş-ay görevi 0 (yeni seed). Build 0/0, site 302, v1.12.3.
- **Not (tasarım kararı):** "1 hafta" değil "**tamamlandığı AY geçince**" tercih edildi (aylık rapor akışına uygun). Kapatıldı = arşiv: aktif görünümlerden çıkar ama raporlarda tamamlanmış sayılır. İstenirse manuel kapatma/geri-açma da mümkün (Detail'de kapalıysa "Kapatıldı (arşiv)" seçeneği görünür).

## Manager: raporlarda da admin gibi tüm kişi/takım kapsamı (commit bc1c769, v1.12.4)
Kullanıcı: "Manager rolü tüm personelleri + tüm projeleri görecek, tüm kişilerin kişisel/ekipsel raporlarını çekebilecek; sadece admin panel ayarlarını yapmayacak".
- **Kök neden:** Manager zaten tüm personel/projeyi görüyordu (`EmployeeAppService`/`ProjectAppService.TeamScopeAsync` → Manager `(false,null)`), AMA `ReportsController.IsManager()` = `Admin||TakımLideri` idi → **Manager raporlarda Uzman gibi** davranıp yalnız kendi raporunu görüyordu.
- **Çözüm (`ReportsController`):** `SeesAll() = Admin||Manager` (tüm kişi + tüm takım raporu, admin gibi); `IsManager() = SeesAll()||TakımLideri` (ekip raporu çekebilen). Tüm-takım kapsamındaki 5 `IsAdmin()` kullanımı → `SeesAll()`: ScopedEmployees (tüm personel), kişisel-rapor yetki kontrolü, ekip-rapor `TeamId=null`, kişisel+ekip Excel export.
- **Admin ayarları Manager'a KAPALI (değişiklik gerekmedi, teyit edildi):** `AdminController [Authorize(Roles="Admin,TakımLideri")]` Manager'ı engeller; Admin menüsü `Can("Admin") && (Admin||TakımLideri)` koşuluyla Manager'a gizli; `RolePageAccesses`'te Manager|Admin=0, Manager|Tasks=0 (izinli: Employees/Projects/Reports + Board/MyTasks/Activities/Work/Requests/TaskQuery/DailyEffort/Dashboard).
- **Doğrulama (canlı):** admin rapor uçları (/Reports/Personal, /Reports/Team) 200, Kişisel form personel dropdown 7 (tümü) — Manager aynı `SeesAll` dalını kullanır. Not: ActAs rol claim'ini Admin tuttuğu için Manager rolü canlıda ActAs ile birebir simüle edilemez; mantık admin ile bire bir aynı dal. Build 0/0, site 302, v1.12.4.

## Login-as: temsil edilen kişi Manager/Admin ise tüm takımları görür (commit 73e5018, v1.12.5)
Kullanıcı: "Personelleri göremiyor; Recep Şahin seçince takımsız olduğu için sadece kendisini görüyor, ama herkesi görmeli".
- **Kök neden (ActAs):** Admin panelden "Recep Şahin (Manager, takımsız)" olarak geçilince kapsam kodu Manager'ı **rol claim'inden** anlıyordu; ActAs'ta rol claim **Admin kalıyor** ve admin-self olmadığı için temsil edilen kişinin **takımına** (Recep takımsız → null) filtreleniyordu → yalnız takımsızlar (= kendisi). Gerçek Manager (Google) girişinde rol claim=Manager olduğundan sorun yoktu; kusur yalnız login-as senaryosundaydı.
- **Çözüm:** kapsam belirleyen tüm servisler artık login-as ile **temsil edilen kişinin gerçek `AppRole`'üne** bakar; Manager/Admin ise tüm takımları görür (rol claim Admin kalsa bile). Değişen: `EmployeeAppService`/`ProjectAppService.TeamScopeAsync` (mevcut TeamId sorgusuna AppRole eklendi — ekstra sorgu yok), `TaskItemAppService`/`ActivitySubjectAppService`/`ServiceRequestAppService.SeesAllTeams` (yalnız ActAs dalında istek-başına cache'li `CurrentEmployeeAppRole` lookup). Reports zaten ActAs'ta rol claim=Admin ile SeesAll oluyordu.
- **Doğrulama (canlı):** ActAs Recep(Manager,takımsız) → `Employee/GetAll` totalCount **1 → 8** (=admin-self); ActAs Aykut(Uzman,Infra) → **6** (kendi takımı, doğru kapsam). Aktif personel 9 = 1 sistem + 8 non-system (sistem hesabı sızmıyor). Build 0/0, site 302, v1.12.5.

## Entegrasyon: destek.cmit.com.tr PULL açıldı + nextPage/sayfalama fix (commit c1df966, v1.13.1)
Kullanıcı iki portal için entegrasyon spec'i verdi: **destek.cmit.com.tr** (Dış Destek) bizim §3.2 şemamıza uyumlu; **psm.tdv.org** (Sunucu Kurulum, KurulumTalepleri) farklı şema (`id/requestedByEmail/assignedToEmail` + zorunlu **ikinci header `X-User-Email`**). Karar: PSM "sadece çekme (pull)".
- **destek açıldı (config, DB — kaynakta değil):** IntegrationSources Id=2 (DisDestek) `Enabled=1`, BaseUrl `https://destek.cmit.com.tr/api/talepler`, `AuthHeader=Authorization`/`AuthScheme=Bearer`, anahtar (64-hex Bearer token) **DPAPI şifreli** (admin `SaveIntegrationSource` endpoint'inden geçirildi, düz metin diske yazılmadı). Global `SyncEnabled=1`, **`IntervalMinutes=1`**. Filter boş (token grup-kapsamlı).
- **Bulunan + düzeltilen hata (`RequestSyncHostedService`):** ilk tur `"The JSON value could not be converted to System.String. Path: $.nextPage"` → portal `nextPage`'i **sayı** dönüyor, `WireResponse.NextPage` `string` idi. Düzeltme: `NextPage` → `JsonElement?` (her tip tolere), zarf esnek (`items` VEYA `data` → `Effective`). Ayrıca **sayfalama** eklendi: `FetchAsync` `page=1..N` (`&page=N`) döngüsü, dedup (externalRef/id) ile portal page'i yok saysa bile sonsuz döngü yok, son sayfa=0/yeni-yok/<50. `WireItem`'a `int? Id` (PSM/dedup).
- **Doğrulama (canlı):** yeni tur **OK: 112 kayıt**; DB'de Source=1 (DisDestek) **112 ServiceRequest** (ExternalRef/Title/RequesterEmail/Status dolu). 112>50 → sayfalama da doğrulandı. Build 0/0, site 302, v1.13.1.
- **PSM (psm.tdv.org) — HÂLÂ KAPALI (Id=1):** açmak için 2 bilgi bekleniyor: (1) `X-User-Email` için aktif servis hesabı, (2) liste yanıt zarfı (`{items}` mı `{data,total,page}` mı). Gelince: IntegrationSource'a `UserEmail` alanı + migration + `X-User-Email` header + PSM alan eşlemesi (`id→ExternalRef`, `requestedByEmail→RequesterEmail`, `assignedToEmail→AssigneeEmail`, donanım→ExtraInfo) uygulanacak. BaseUrl'de tire farkı da düzeltilecek (`/api/kurulum-talepleri` → `/api/kurulumtalepleri`).

## Talep detay HTML render + Destek sekmesi önde + efor izni yöneticiye (commit 16e0b8b, v1.13.2)
- **Açıklama HTML render (sanitize):** destek portalı açıklamayı HTML (`<p>/<strong>/<a mailto>`) gönderiyor; [Requests/Detail.cshtml] eskiden `WebUtility.HtmlEncode` ile **ham tag** gösteriyordu. Yeni `ActivityManagement.Web.Helpers.SafeHtml.RenderDescription`: `<script>/<style>/<iframe>/<form>/on*=/javascript:` temizler (XSS koruması, dış içerik), biçimlendirmeyi korur; düz metinse encode + `<br/>`. Doğrulama (canlı): `<p>` render edildi, `&lt;p&gt;`/`<script>` yok.
- **Talepler sekme sırası:** Destek **1. sıra + aktif**, Sunucu 2. sıra (kullanıcı: "destek daha fazla gelecek"). Doğrulama: `#tab-destek , #tab-sunucu`, aktif=destek.
- **Talep efor izni genişletildi:** eskiden efor **yalnız atanan kişi** girebiliyordu; artık **atanan / 2. sorumlu / kapsamındaki yönetici** (Admin/Manager/TakımLideri) da girebilir. `CanLogEffort` (DTO) + `LogEffortAsync` guard birlikte; `EmployeeId` yoksa dostça hata. (CLAUDE.md "yönetici kendi kapsamında işlem yapar" ile uyumlu.)
- **Geçmiş efor / raporlama kararı:** destek §3.2 payload'ı **efor/saat İÇERMEZ** (sadece meta: başlık/durum/tarih/kişi). Dolayısıyla geçmişe dönük (30 gün) çekilen taleplerin eforu **otomatik gelmez** → manuel girilir. Efor formundaki **Tarih** alanı geçmiş tarih kabul eder; raporlar `ActivityDate`'e göre saydığı için geçmiş tarihli efor geçmiş raporlara düşer. (İleride portal API'sine saat alanı eklenirse otomatik içe aktarım yapılabilir.)

## Talep görünürlüğü kişisel + 30 gün backfill + Description nvarchar(max) (commit 89b5201, 2bfec19; v1.13.3/1.13.4)
Kullanıcı: "entegrasyon hepsini çeker de eşleşme ile personellere göstermesi lazım; Aykut'un talep sayfasında başka talepler olmamalı" + "ilk geri bakış 30 ayarladım ama 7 gün gibi çekiyor".
- **Görünürlük kişiselleştirildi (`ServiceRequestAppService.GetAllAsync`, v1.13.3):** eskiden talep görünürlüğü TAKIM bazlıydı (Uzman tüm takımının taleplerini görüyordu). Artık **SeesAllTeams (Admin/Manager) tümünü + havuzu** görür; **diğer personel yalnız kendine (1./2. sorumlu) atanan** talebi görür. Doğrulama: admin 374/113, ActAs Aykut(Uzman) 2, İsmail 2.
- **Eşleşme doğru:** destek `assigneeEmail` = `@cmit.com.tr`, personel e-postasıyla tam eşleşir (kullanıcı teyit). Ek mapping gerekmez; eşleşmeyen (havuz) talepler yönetici/admin'de kalır, atanınca ilgili kişiye düşer.
- **"İlk geri bakış" mekanizması:** `InitialLookbackDays` yalnız **watermark (LastSyncUtc) boşken** çalışır. İlk sync watermark'ı doldurunca sonraki turlar `updatedSince=LastSyncUtc` (incremental) kullanır → 30 günü geriye çekmez. **30 gün backfill için watermark sıfırlanır** (`LastSyncUtc=NULL`); upsert idempotent (kopya olmaz).
- **Backfill truncation fix (migration V11, v1.13.4):** watermark sıfırlanınca 30 gün çekilirken `String or binary data would be truncated: ServiceRequests.Description` (bir HTML açıklama >4000) TÜM partiyi düşürüyordu. Çözüm: **Description nvarchar(4000)→nvarchar(max)** (V11, açılışta oto-uygulandı, `max_length=-1` doğrulandı) + `UpsertFromPortalAsync`'te portal alanlarını kolon sınırına kırpma (`Trunc`: Title/ExternalRef/Url/Requester*/ExtraInfo/ActivityType).
- **Sonuç (canlı):** 30 gün backfill **OK: 374 kayıt** (113→374; sayfalama ~8 sayfa). 18 atanmış (ekip), 356 havuz. Watermark ilerledi → steady-state 1 dk incremental. Build 0/0, site 302.

## Talep: kapalı ilerleme %100 + Efor göstergesi (commit 320ca18, v1.13.5)
- **Kapandı/Çözüldü → %100:** portaldan zaten kapalı gelen talepler INSERT'te `CompletionPercentage=0` kalıyordu (transition bloğu tetiklenmiyordu). `UpsertFromPortalAsync` insert'te artık `CompletionPercentage = ProgressForStatus(initStatus, 0)` → Kapandı/Çözüldü %100. Mevcut **355 kapalı/çözülmüş kayda tek seferlik `UPDATE ... =100`** uygulandı (hepsi 100 doğrulandı).
- **Efor göstergesi (`_RequestTable`):** yeni "Efor" kolonu — efor>0 yeşil "X sa"; **Kapandı/Çözüldü ama efor yok → kırmızı "Efor yok"** (rapor için nudge; efor çıktısı önemli); açık+efor yok → nötr "—" (356 havuzu kırmızıya boğmadan).
- **PSM (Sunucu Kurulum) — BEKLİYOR:** kullanıcı tam API dosyalarını iletecek; "bizim sistemde `KurulumTalepleriController` olması lazım" dedi → yön netleştirilecek (bizim sistem bu API'yi EXPOSE mu edecek, yoksa PSM'den PULL mu). Dosyalar gelince uygulanacak.

## PSM (Sunucu Kurulum) PULL CANLI + HTTP→HTTPS yönlendirme (commit decb430, 6f0d34c; v1.14.1/1.14.2)
- **Yön:** B (PSM sunar, biz çekeriz). Gerçek uç: **`https://psm.tdv.org/fm/api/kurulumtalepleri`** (`/fm/` önekli, tiresiz).
- **Kimlik (canlı testle bulundu):** `X-Api-Key: Sistem2026!..APIKey` (düz metin; PSM kendi `ENC:` ile karşılaştırır — PSM appsettings'teki `KurulumTalepleriApiKey":"ENC:..."` bize gönderilMEZ, çözemeyiz) + `X-User-Email: aykut.ayvaz@tdv.org` (geçerli aktif PSM kullanıcısı; CMIT e-postaları PSM'de yok). Yanıt zarfı: `{success,total,page,pageSize,items:[...]}`.
- **Kod (v1.14.1):** `IntegrationSource.UserEmail` (migration V12) + admin ekranı "Aktör E-posta" alanı → FetchPageAsync ikinci header `X-User-Email`'i ekler. X-Api-Key mevcut AuthHeader(=X-Api-Key)/AuthScheme(boş) ile. PSM alan eşlemesi (`WireItem`/`MapWire`): `id→ExternalRef`, `requestedBy*→Requester*`, `assignedToEmail→AssigneeEmail`, donanım (hostname/ip/os/cpu/ram/disk/env/lokasyon/atanan)→ExtraInfo. `from=<date>` de gönderilir. Esnek zarf (items/data + tolerant nextPage + page döngüsü).
- **Sonuç (canlı):** PSM kaynağı (Id=1) açıldı; ilk sync **OK: 3 kayıt** → Source=0 ServiceRequest (ExternalRef=id, Kapandı→%100, Tip=Kurulum, donanım ExtraInfo'da). Build 0/0.
- **HTTP→HTTPS (v1.14.2):** IIS'te URL Rewrite modülü YOK → `Startup`'ta ASP.NET Core middleware ile http→https **301**, yalnız `*.tdv.org` host'ları (localhost:8090 test/izleme etkilenmez; in-process ANCM IsHttps doğru → döngü yok). Doğrulama: localhost=200, `Host: activitymanagement.tdv.org` (http)=301→https.

## Portal e-posta eşleşmesi: yerel-parça (domain gözardı) (commit 9bf6d19, v1.14.3)
Kullanıcı: "PSM'deki `aykut.ayvaz@tdv.org` ↔ bizdeki `aykut.ayvaz@cmit.com.tr` eşleşmeli; @tdv.org/@cmit.com.tr gözardı, ön ek yeter; PSM'den üstümde 2 talep olmalı." Sorun: PSM assignee'leri `@tdv.org`, bizim personel `@cmit.com.tr` → **tam e-posta eşleşmesi domain yüzünden başarısız** → PSM talepleri atanmamış → kişisel görünürlükte gözükmüyordu.
- **`ResolveEmployeeByEmailAsync`:** önce tam eşleşme; bulunamazsa **domain gözardı**, `@` öncesi **yerel-parça** (`EmailLocalPart`) ile eşleşir. Hem PSM (@tdv.org) hem destek (@cmit.com.tr) için geçerli.
- **Upsert güncelleme:** atanan boşsa ve portal artık eşleşen kişi veriyorsa doldurulur (manuel atamayı EZMEZ). Mevcut 3 PSM kaydı için PSM watermark sıfırlandı → re-sync atadı.
- **Doğrulama (canlı):** 3 PSM talebi atandı — ExternalRef 2&3 → Aykut (Id 2), ExternalRef 1 → Mustafa (Id 4). ActAs Aykut(Uzman) Sunucu sekmesi = **2** (admin=3). Build 0/0, v1.14.3.

## İşlerim (/Work) taleplerim görünsün (commit 8c51bb2, v1.14.4)
Kullanıcı: "/Work tarafında taleplerde görünmeli." Sorun: `/Work` talepleri `OnlyOpen=true` ile çekiyordu; kullanıcının backfill'den gelen tüm talepleri **KAPALI** → hiçbiri görünmüyordu.
- **`WorkController`:** taleplerim `MineOnly` ile çekilir; **açık olanlar + kapalı olup eforu girilmemiş** (`IsOpen || TotalHours<=0`) olanlar listelenir. Kapalı-eforsuz satır kırmızı **"· efor bekliyor"** etiketiyle (rapor için efor gerekli; efor girilince /Work'ten düşer → anlamlı, kendini sınırlayan iş listesi).
- **Doğrulama (canlı):** ActAs Aykut → /Work'te **9 talep**, "efor bekliyor" ×9.

## Repoya özel Claude Code becerileri (.claude/skills, commit 9e0c3f3)
Kullanıcı obra/superpowers'ı sordu → "B: repoya özel skill yaz" seçildi. superpowers metodolojisinden (brainstorm→plan→uygula→KANITLA→review; kanıt>iddia) esinlenip **dışarıdan kod çekmeden**, bu repoya uyarlanmış 5 beceri + README yazıldı: `brainstorm-plan-verify` (ana yöntem), `abp-deploy`, `git-commit-push`, `code-review`, `db-query`. `.gitignore`: `.claude/` → `.claude/*` + `!.claude/skills/` (yalnız beceriler versiyonlanır; yerel ayar/oturum ignore kalır). App kodu değil → build/deploy/sürüm yok. Tam framework isteyen: CLI'de `/plugin install superpowers@claude-plugins-official`.

## Talep UX + görev sorumlu ön-seçimi (commit 84b5fcf, 8151b78; v1.14.4/1.14.5/1.15.1)
- **/Work taleplerim (v1.14.4→1.14.5):** önce açık+kapalı-eforsuz gösterildi; kullanıcı "kapalılar İşlerim'de olmasın" deyince **yalnız açık** yapıldı (yapılacaklar temiz). Efor eksik kapalılar Talepler'deki "Efor yok" rozetinden takip edilir.
- **Talep sekme badge'i (v1.14.5):** `IsOpen` yerine **toplam** (`.Count`); liste zaten görünürlük-kapsamlı → admin=tümü (Destek 377/Sunucu 3), Uzman=kendi (ActAs Aykut: Destek 7/Sunucu 2). Doğrulandı.
- **Görev kapatma:** OTOMATİK (TaskAutoCloseHostedService, tamamlandığı ay geçince); durum dropdown'ında manuel "Kapatıldı" yok (arşiv).
- **Görev oluşturmada 1./2. sorumlu (v1.15.1):** seçilen **alt kategorinin sorumluluk matrisinden** (`SubCategoryResponsibility`: Asıl=Primary→1., Yedek=Backup→2.). `TasksController.BuildSubCatRespMapAsync` + tree'den subCategoryId geldiğinde sunucu ön-doldurma + `Create.cshtml` JS. Matris yoksa ana kategori sorumlularına düşer; kendine görevde dropdown yok → etkilenmez. Doğrulama: Create?subCategoryId=102 → 1.=Mustafa(Asıl), 2.=Aykut(Yedek).
- **Atayan Kişi otomatik (v1.15.2→1.15.3):** `CreateAsync` AssignedBy'ı HER ZAMAN `ctx.EmployeeId` (oturum açan / login-as temsil edilen) yapar; form değeri yok sayılır (spoof güvenli). Create formunda Atayan dropdown kaldırıldı → salt-okunur "otomatik" bilgi (ActingAsName). Kanıt: formdan assignedBy=4 gönderildi, oluşan görev assignedBy=7.
- **/Work efor-yok talepler (v1.15.3):** geçen turku düzeltme — İşlerim'de AÇIK + KAPALI-EFORSUZ ("efor bekliyor") görünür; tam bitmiş (kapalı+eforlu) gizli. Kullanıcı: "efor yok olanlar görünmeli yoksa rapor boş kalır." (`IsOpen || TotalHours<=0`). Doğrulama: ActAs Aykut → 9 talep, "efor bekliyor" ×9.
- **Görev efor girişine tarih (v1.15.4):** görev detayındaki efor+yorum kutusunda tarih yoktu (efor hep `DateTime.Today`'e işleniyordu). `type=date` `effortDate` alanı (varsayılan bugün) + `TasksController.AddComment` param → `LogEffortAsync.ActivityDate`. Talep/Faaliyet/Günlük Efor formları zaten tarihliydi. Çoklu gün: `LogEffortAsync` her çağrıda InsertAsync (tek-kayıt kısıtı yok) → aynı göreve/talebe farklı günler ayrı efor. Doğrulama: görev 1'e 2026-07-20 + 2026-07-24 = 2 ayrı kayıt kendi tarihine (test temizlendi).

## Talep Sorgula + Kapatılan sekmesi + pull bildirim + portal status sync (commit 8974fbc, v1.16.1)
- **A) Talep Sorgula (`/Requests/Query` + `ExportQueryExcel`):** TÜM talepler (arşiv/kapalı dahil) kaynak/durum/kişi/metin filtreli, açık+kapalı aynı ekranda (Görev Sorgula muadili). Menüde Talepler dropdown'ında. `_RequestTable`'a `ViewData["ReadOnly"]` (sorgu ekranı yalnız Detay). Doğrulama: status=Kapandı → 353 kayıt, HTTP 200.
- **B) Pull atama bildirimi:** `UpsertFromPortalAsync` YENİ + AÇIK bir talebi personele atadığında zil (`TalepAtandi`). Backfill'deki kapalı/eski talepler bildirilmez (isOpen); her senkronda tekrar bildirilmez (yalnız yeni/ilk atama: `isNew || wasUnassigned`).
- **C) Portal status yansıması:** `Çözüldü/Kapandı/İptal` portaldan gelince yerel duruma yansır (önceden yalnız Kapandı/İptal → Çözüldü yansımıyordu). Ara durum (Devam/Beklemede) yerelde korunur; yerelde zaten Kapandı/İptal ise geri açılmaz. Destek'te "Çözüldü" → bizde Çözüldü (%100).
- **D) Kapatılan Talepler sekmesi:** Kapandı + eforu girilmiş (tam bitmiş) talepler 3. sekmede (arşiv, `bg-dark` badge); Destek/Sunucu sekmelerinde görünmez (`RequestsController.Index` böler: `Status==Kapandi && TotalHours>0` → Kapatılan). Doğrulama: Destek 391 / Sunucu 2 / Kapatılan 1.

## Talep durumu PORTAL AUTHORITATIVE (tam ayna) (commit 28fe9e3, v1.16.2)
Kullanıcı: "talep durumunu kendi sistemimizden düzenlemeyelim, güncel durumu hep destekten çekelim; efor girmek yeterli." Karar: pull modelinde yaşam döngüsü portalda → biz aynasıyız.
- `UpsertFromPortalAsync`: durum **tam ayna** — her senkronda portalın güncel durumu yansır (tüm durumlar; `CompletionPercentage=ProgressForStatus`; yeniden açılırsa kapanış/çözüm tarihleri temizlenir; eşlenemezse korunur). `MapStatusText` destek TR + PSM EN durumlarını kapsıyor.
- `UpdateStatusAsync`: **ExternalRef dolu (portal) talepte durum güncelleme reddedilir** ("destek portalından güncellenir; burada yalnız efor"). Manuel talep (ExternalRef yok) eskisi gibi.
- UI: Detay'da portal talebinde "Durum Güncelle" yerine bilgi notu; `_RequestTable` Durum butonu yalnız manuel talepte. `_RequestTable` ReadOnly bayrağı tüm işlem butonlarını gizler (Talep Sorgula).
- Doğrulama: portal talebine UpdateStatus → guard mesajı, durum değişmedi.
- **Açık soru (kullanıcı brainstorm istedi):** ters yön — bizde durum/yorum girince destek'e POST ile yansıtma (write-back). Kod YAZILMADI; tasarım tartışılıyor (aşağıdaki notlar sohbet kaydında).
