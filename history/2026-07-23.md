# ActivityManagement — Çalışma Geçmişi (23.07.2026)

> Kaynak `C:\ActivityManagement`, Canlı `C:\inetpub\ActivityManagement` (IIS in-process, port 8090). Backlog: `23072026-gelistirme.md`.
> Dünün (2026-07-22) tüm işleri canlıda — bkz. `history/2026-07-22.md`.

## Bugünün Planı (23072026-gelistirme.md TODO)

- **A. Toast + Tarih:** Sağ-üst overlay toast bildirim (alert-banner kaldır); `dd.MM.yyyy` culture doğrulama/pekiştirme.
- **B. MyTasks:** Öncelik sıralaması + Auto-Refresh toggle (default AÇIK) + akıllı form (TreeView seçili kategori otomatik, Uzman takım kilitli, süre default 1s + validation).
- **C. Görev Sorgula + Rol İzolasyonu:** "Tamamlanmış Görevler" → "Görev Sorgula" (tüm durumlar, rol filtreli: Uzman→kendi, Lider→takım, Admin→hepsi); Raporlama rol yetkisi; Dashboard rol-bazlı görünürlük.
- **D. Proje Sorumluları + SLA:** Project'e 1./2. Sorumlu + SlaTargetDate (migration); proje görevinde sorumlu oto-atama + SLA son tarih mirası.
- **Doğrulama:** Audit/Gantt/RichText/Faaliyet dün tamamlandı — kontrol.

## İlerleme — TÜM FAZLAR CANLIDA ✅

### A — Toast + Tarih (CANLIDA)
- Sağ-üst overlay toast: `site.js`'teki `abp.notify` shim (position:fixed top-right, auto-dismiss) zaten uygundu. `_Layout`'taki alert-banner'lar kaldırıldı; TempData Success/Error/Uyari artık tek noktadan (bottom script) `abp.notify.[success/error/warn]` toast olarak gösteriliyor. 7 view'daki tekrarlı TempData banner'ları temizlendi.
- Tarih: global `tr-TR` culture (dünden) korunuyor → `dd.MM.yyyy`.

### B — MyTasks (CANLIDA)
- Öncelik sıralaması `GetEmployeeTasksAsync` (Priority desc) ile zaten geliyor.
- **Auto-Refresh toggle** (default AÇIK, localStorage'da hatırlanır, 30 sn'de bir `location.reload`).
- **Akıllı "Yeni Görev":** soldaki TreeView'da seçili kategori/alt kategori `Create?categoryId=..&subCategoryId=..` ile forma taşınır; Create view'ı bunları otomatik ön-seçer.
- EstimatedHours default 1 (Create GET) + backend `<=0 ise 1` guard.

### C — Görev Sorgula + Rol İzolasyonu (CANLIDA)
- "Tamamlanmış Görevler" → **"Görev Sorgula"** (menü + başlık). Artık tüm durumlar; **Durum filtresi** + Durum kolonu eklendi (forced Tamamlandi kaldırıldı).
- **Rol filtresi** (`ApplyRoleScopeAsync`): Admin→tümü, TakımLideri→kendi takımı (`TeamId`), Uzman→sadece kendi görevleri (`AssignedEmployeeId`). Uzman'da personel filtresi kilitli. Excel export da rol filtreli.
- **Rapor rol yetkisi** (`ReportsController`): Uzman→sadece kendi kişisel rapor; Lider→kendi takımı (personel listesi + team report `GetReportInput.TeamId` ile sınırlı); Admin→hepsi. URL ile kapsam dışı kişi çekme engellendi. Team report Uzman'a kapalı.
- **Dashboard izolasyon** (`Home/Index`): Admin tüm veri; Lider/Uzman yalnızca kendi takımının personel/proje/görev sayıları ve listeleri.

### D — Proje Sorumluları + SLA (CANLIDA)
- **Schema:** `Project`'e `PrimaryResponsibleId`, `SecondaryResponsibleId`, `SlaTargetDate` (migration `AddProjectResponsiblesAndSla`, doğrulandı). `ManagerId` korunuyor ve **1. Sorumlu ile senkron** tutuluyor (mevcut yetki mantığı ManagerId üzerinden).
- **Proje Create/Edit:** "Proje Yöneticisi" → **1. Sorumlu + 2. Sorumlu** (tekil kişi) + **SLA / Hedef Bitiş** tarihi.
- **Görev oto-atama + SLA mirası** (`TaskItemAppService.CreateAsync`): ProjectId varsa, atanan sorumlular boşsa projenin 1./2. sorumlusundan; son tarih boşsa projenin `SlaTargetDate` (yoksa `PlannedEndDate`) tarihinden otomatik doldurulur → "son tarihi olmayan görev" engellendi.
- **Arayüz ayrımı:** Proje görevinde (Create?projectId=) Son Tarih alanı gizli (SLA'dan miras); standart görev ekleme sayfasında görünür.
- **Proje Detay:** "Yönetici" satırı → 1./2. Sorumlu + SLA/Hedef gösterimi; üye-ekleme modalındaki "Proje Yöneticisi" checkbox'ı kaldırıldı (§3).
- **Görünüm modları (§6.1):** /Tasks (admin) toolbar'ına Liste/Pano/**Gantt** geçişleri tamamlandı.

## Sonuç: 23072026-gelistirme.md TODO'nun 8 maddesinin TAMAMI canlıda ✅
(1 tarih, 2 rol-yetki, 3 SLA-oto, 4 akıllı form, 5 uzman-oto+validation, 6 toast, 7 MyTasks öncelik+auto-refresh, 8 audit/faaliyet/gantt/richtext [dün] — hepsi tamam.)

## DB Geçişi — AlwaysOn AG'ye taşındı (CANLIDA) ✅

- **Hedef:** AG listener `tdvsqlagl1tr1` (AG `tdvsqlag1tr1`; 3 replica SYNCHRONOUS + AUTOMATIC seeding; `infra` sysadmin). Eski `192.168.31.31` terk edildi (veri taşınmadı, temiz seed).
- **AG DDL (CREATE DATABASE / ALTER AVAILABILITY GROUP) Claude Code güvenlik katmanınca engellendi** (üretim AG koruması) → kullanıcı SSMS'te çalıştırdı: DB oluştur + FULL recovery + AG'ye ekle (auto-seed).
- **App tarafı (ben):** yeni bağlantı dizesini DPAPI ile şifreleyip `appsettings.Production.json`'a yazdım (`MultiSubnetFailover=True`), app restart → `Migrate()` **17 migration + 52 tablo + seed** (13 kategori, 61 alt kategori, 6 çalışan, admin) primary'de kurdu; AG ile TDVSQL2TR1'e replike oldu.
- **Sonuç:** Primary + TDVSQL2TR1 **SYNCHRONIZED** (2 node HA, kullanıcı 3. node'u istemedi). HTTP 200. Kaynak `appsettings` + CLAUDE.md güncellendi (`dbq.ps1`/`dbexec.ps1` artık yeni cluster'ı sorgular).
- **Yedek:** eski `appsettings.Production.json` → `scratchpad\backup2\appsettings.Production.json.bak` (gerekirse geri dönüş).

## 23072026-gelistirmev2.md (v3 güncellemesi) — TÜM FAZLAR CANLIDA ✅

Aynı gün, DB geçişinden sonra `23072026-gelistirmev2.md` (Bölüm 2–3 & 6, TODO) uygulandı ve AG cluster'a deploy edildi.

### v2-P1 — Projede Zorunlu Ana/Alt Kategori + göreve kalıtım/kilit (CANLIDA)
- `Project`'e `CategoryId`/`SubCategoryId` (migration `AddProjectCategories`, FK Restrict). Create/Edit formlarında **Ana Kategori + Alt Kategori zorunlu** (cascade dropdown, `JsonHelper.ToCamelCaseJson`).
- **Görev kalıtımı** (`TaskItemAppService.CreateAsync`): `ProjectId` varsa görevin `SubCategoryId`'si projenin alt kategorisinden **kilitli miras** alır (override). Create GET `projectId` gelince kategori+atananlar ön-dolar ve UI'da kilitlenir (`ViewBag.FromProject`, `disabled="@(fromProject?"disabled":null)"`).

### v2-P2 — ActivitySubject.ProjectId + faaliyette projeden kategori oto-doldurma (CANLIDA)
- `ActivitySubject`'e `ProjectId`/`Project` (migration `AddActivitySubjectProject`, FK SetNull). `ActivitySubjectAppService`'e `IRepository<Project>` + `ApplyProjectCategoryAsync` (proje seçilince Category/SubCategory otomatik dolar), Create/Update'te `ProjectId` set, DTO'lara `ProjectId`/`ProjectName`.
- `Activities/Index.cshtml`: **Proje dropdown** → seçilince kategori/alt kategori otomatik dolar.

### v2-P3 — Alt Kategori Sorumluluk Matrisi (Asıl/Yedek) (CANLIDA)
- **Yeni entity** `SubCategoryResponsibility` (`Id, TenantId, SubCategoryId, EmployeeId, ResponsibilityType[Primary/Backup], AssignedByTeamLeaderId`) + migration `AddSubCategoryResponsibility` (unique index `SubCategoryId+EmployeeId`; FK SubCategory=Cascade, Employee=Restrict, AssignedByTeamLeader=NoAction → çoklu cascade yolu hatası önlendi).
- **AppService** `SubCategoryResponsibilityAppService` (manuel claim auth: yalnız Admin/TakımLideri): `GetAllAsync`, `GetByEmployeeAsync`, `SetAsync` (SubCategoryId+EmployeeId unique upsert), `RemoveAsync`.
- **Ekran** `Admin/Responsibilities.cshtml` (sol: kategori-cascade + personel + Asıl/Yedek atama formu; sağ: mevcut matris tablosu + kaldır). `Admin/Index`'e **"Sorumluluk Matrisi"** kartı. `AdminController`: `Responsibilities/SetResponsibility/RemoveResponsibility`.
- **Kişi Kartı** (`Employees/Card.cshtml` + `EmployeesController.Card`): "Sorumluluk Alanları" kartı — kişinin Asıl/Yedek sorumlu olduğu alt kategoriler (kategori/alt kategori + Asıl/Yedek badge). İki yönlü görünüm: matris (kategori tarafı) + kişi kartı (kişi tarafı).

### v2-P4 — Atayan Kişi otomasyonu + Kişi Kartı proje fix + Üye ekleme UI (CANLIDA)
- **AssignedBy otomasyonu** (`TaskItemAppService.CreateAsync`): Uzman kendine açarsa atanan+atayan=kendisi; Lider/Admin atarsa ve `AssignedByEmployeeId` boşsa **atayan=işlemi yapan yönetici**.
- **Kişi Kartı proje fix** (`EmployeeAppService.GetCardAsync`): `IRepository<Project>` eklendi; artık kişinin projeleri **hem ProjectEmployee üyeliği hem de 1./2. sorumlu olduğu projeler** birleştirilerek (dedup, sorumluluk rolü üyelikten öncelikli) listeleniyor → "projeye eklenen kişinin kartında proje görünmeme" sorunu çözüldü.
- **Üye ekleme UI** (`Projects/Detail.cshtml` + `ProjectAppService.AddMemberAsync(..., int responsibilityLevel)`): modalda **Sorumluluk radyosu** (Üye / 1. Sorumlu / 2. Sorumlu). 1./2. seçilince rol otomatik ("1./2. Sorumlu") ve proje `PrimaryResponsibleId`/`SecondaryResponsibleId` (1.'de `ManagerId` de) güncellenir; diğer üyelerde serbest **Rol/Unvan** girişi.

### v2 Deploy & Doğrulama
- Release build **0 uyarı/0 hata**. Publish → `app_offline.htm` → 4 DLL (Core/Application/EntityFrameworkCore/Web.Mvc) + pdb kopya → `app_offline` kaldır. (Views ana `.Web.Mvc.dll`'e gömülü — ayrı Views.dll yok.)
- Yedek: `scratchpad/backup2/20260723_100741/` (deploy öncesi canlı DLL'ler).
- Doğrulama: HTTP 302 (login). AG cluster'da `SubCategoryResponsibilities` tablosu + `AddSubCategoryResponsibility` migration **uygulandı** (dbq: TableExists=1, MigrationApplied=1). Error log **boş** (hata yok).

## Bug Fix — Pano (Board) boş geliyordu → WorkflowStatuses seed eksikti (CANLIDA) ✅
- **Belirti:** Aykut Ayvaz'da (ve aslında herkeste) `/Tasks/Board` boş; MyTasks'ta aktif görevler var.
- **Teşhis (veriye bakarak):** Aykut = EmployeeId 2, Uzman, TeamId=NULL, 3 görevi var (durum 0/1/2, hepsi kendine atanmış). `WorkflowStatuses` tablosu **0 satır**. Pano kolonları `WorkflowStatus/GetAll` (onlyActive) + `TaskItem/GetAll` (yalnız projectId) ile kuruluyor; görevler `t.status === st.statusValue` ile kolonlara dağılıyor. Durum tablosu boş → hiç kolon → pano tamamen boş. (Board AppService `GetAllAsync`'i doğrudan çağırıyor, controller rol filtresini atlıyor; sorun rol/izolasyon değil, durum seed'i.)
- **Kök neden:** `WorkflowStatuses` hiçbir migration'da/SeedDataBuilder'da tohumlanmıyordu; eski DB'de elle (Admin → Statü) oluşturulmuştu. Temiz AG seed'inde kalmadı.
- **Düzeltme:** `SeedDataBuilder.SeedWorkflowStatuses()` eklendi (boşsa 5 varsayılan durum: Beklemede/Devam Ediyor/Tamamlandı/İptal/Ertelendi, `StatusValue`=0..4 `TaskStatus` enum ile eşleşir, renkler MyTasks ile aynı). `Create()`'e eklendi → startup'ta idempotent seed. Deploy sonrası doğrulandı: 5 durum tabloda, StatusValue 0–4. Gelecekteki temiz DB'lerde de otomatik kurulur.

## Infrastructure Team seed + Kişi Kartında Takım Lideri (CANLIDA) ✅
- **İstek:** 6 kişi statik olarak "Infrastructure Team"de olsun; kişi kartında takım lideri Emin Akkurt görünsün.
- **Durum tespiti:** DB'de "Infrastructure" adlı takım (Id 1) vardı ama **LeaderId=NULL ve hiçbir çalışanın TeamId'si atanmamıştı** (hepsi NULL — Board rol filtresini de etkiliyordu).
- **Seeder** (`SeedDataBuilder.SeedTeam()`, idempotent): mevcut "Infrastructure"/"Infrastructure Team" satırını onarır (ad→"Infrastructure Team", `LeaderId`→Emin Akkurt), takımı olmayan tüm çalışanları bu takıma bağlar (başka takıma taşınmışlara dokunmaz). `Create()`'e eklendi → startup'ta çalışır.
- **Kişi Kartı:** `EmployeeDto`'ya `TeamLeaderId`/`TeamLeaderName`; `GetCardAsync` `Team.Leader` include eder ve doldurur; `Card.cshtml`'de Takım badge'i + "Takım Lideri: Emin Akkurt" (lider kartına link) gösterimi.
- **Doğrulama:** Infrastructure Team (Id 1) → Lider **Emin Akkurt (Id 1)**, **6 üye**; tüm çalışanların TeamId=1. HTTP 302.

## Faaliyet Konusu — 3 rol de onaysız açabilir (CANLIDA) ✅
- **İstek:** Admin, Takım Lideri **ve Uzman** — 3 rol de onaysız faaliyet konusu açıp altına efor girebilsin (eskiden yalnız Admin/Lider açabiliyordu).
- **Backend** (`ActivitySubjectAppService.CreateAsync`): manager-only `throw` kaldırıldı. Uzman konu açınca konu **kendisine atanır** (`AssignedEmployeeId = ctx.EmployeeId`) → `CanLogEffort` true olur, altına efor girebilir. Yönetici dilediği uzmana atar (davranış aynı). `GetAllAsync` Uzman filtresi genişletildi: kendisine **atanan VEYA kendi oluşturduğu** konuları görür.
- **UI** (`Activities/Index.cshtml`): "Yeni Faaliyet Konusu" butonu + modal + kategori-cascade script'i artık **tüm rollere** açık (`@if (isManager)` gate'leri kaldırıldı). Modaldaki "Atanan Uzman" seçimi yalnız yöneticide; Uzman'da "Kendinize atanacak" bilgi notu. Efor girişi zaten `CanLogEffort` ile gösteriliyordu (atanan Uzman görür).
- **Doğrulama:** Build 0/0, HTTP 302, error log boş.

## Proje Detay — Faaliyetler (üstte) + Görevler (altta) (CANLIDA) ✅
- **İstek:** Proje detayında hem faaliyet hem görev olabilsin; faaliyetler üstte listelensin. Faaliyet girilirken proje seçilirse o projenin altında listelensin.
- **Backend:** `GetActivitySubjectsInput`'a `ProjectId` filtresi + `GetAllAsync`'te `WhereIf(ProjectId)`. `ProjectsController.Detail` `IActivitySubjectAppService` enjekte edip `ViewBag.Activities = GetAllAsync(ProjectId=id)` yükler (try/catch).
- **Proje Detay view:** Sağ kolonda **Faaliyetler kartı üstte** (server-side tablo: Konu/Kategori/Atanan/Efor-saat+log sayısı, detay linki) + "Faaliyet Ekle" butonu (`/Activities?projectId=`), altında mevcut **Görevler** kartı.
- **Activities Index:** `Index(long? projectId)` → `ViewBag.PreselectProjectId`; modalda proje option ön-seçili gelir, script modalı otomatik açıp proje `change` olayını tetikler (kategori projeden dolar/kilitlenir). Böylece proje detayından "Faaliyet Ekle" → proje hazır modal.
- **Doğrulama:** Build 0/0, HTTP 302, error log boş.

## Faaliyet Yönetimi Röntgeni — 8 madde düzeltme (CANLIDA) ✅
Sistemin faaliyet ekseninde analizinden çıkan 8 madde tek batch'te yapıldı.
- **#1 Efor→proje bağı:** `LogEffortAsync` artık `ActivityLog.ProjectId = subject.ProjectId` set ediyor → projeye bağlı faaliyet eforları raporun proje kırılımında görünüyor.
- **#2 Görünürlük vs işlem:** `ActivitySubject.GetAllAsync` — Admin tümü; **TakımLideri+Uzman kendi takımı** (TeamId; kendi atanan/oluşturduğu dahil). Görev tarafı: `TasksController.ApplyRoleScopeAsync` Uzman'a `AssignedEmployeeId` zorlaması kaldırıldı → Uzman **takım görevlerini görür**, işlem yine `CanEdit` ile kendine sınırlı. `Completed.cshtml` personel filtresi herkese açıldı (takım kapsamlı).
- **#3 Efor tek kapı:** eski `ActivityLogAppService.Create/Delete` yalnız yöneticiye açık (asıl kapı `LogEffortAsync`).
- **#4 Manuel auth standardı:** `ActivityLogAppService`, `EmployeeAppService`, `ProjectAppService`'ten `[AbpAuthorize]` tamamen kaldırıldı → hepsi manuel claim (mevcut EnsureManager/kendi-kayıt guard'ları korunarak). CLAUDE.md **Yetki + Görünürlük/İşlem + Faaliyet** kuralları güncellendi.
- **#5 Faaliyet tipi:** efor formuna **Faaliyet Tipi** dropdown; kişisel rapora **"Faaliyet Tipi Dağılımı (Efor)"** kırılımı (`ActivityTypeBreakdown`, ActivityLog.ActivityType bazlı).
- **#6 Efor silme:** `DeleteEffortAsync` (kendi eforu VEYA yönetici) + `ActivitiesController.DeleteEffort` + Detay efor tablosunda çöp-kutusu butonu.
- **#7 Proje faaliyet listesi:** yeni `GetByProjectAsync(projectId)` (kişisel/takım kapsamına takılmaz) → proje detayında projenin **tüm** faaliyetleri görünür.
- **#8 Kategori zorunlu:** faaliyet projesiz olabilir ama kategorisiz olamaz — `CreateAsync`/`UpdateAsync` doğrulaması + modalda Ana Kategori `required` (proje seçilince otomatik dolar).
- **Doğrulama:** Build 0/0, HTTP 302, error log boş. Yedek `backup2/20260723_120910_faaliyetfix/`.
- **Not (kapsam dışı bırakılan, bilinçli):** Pano (`/Tasks/Board`) hâlâ tüm sistemi gösteriyor (AppService `TaskItem.GetAll`'ı doğrudan çağırıyor, controller filtresini atlıyor). Tek takım olduğundan sızma yok; çok-takıma geçişte Board'a takım kapsamı eklenmeli. TakımLideri'nin *işlem* yetkisi hâlâ global (IsManager) — görünürlük takımla sınırlı ama başka takımın kaydına doğrudan id ile erişilirse yönetebilir; çok-takım senaryosunda daraltılabilir.
  - **[KAPANDI 2026-07-23]** Aşağıya bakınız ("Çok-takım izolasyonu tamamlandı").

## Çok-takım izolasyonu tamamlandı — Board scope + ActivitySubject yönetici kapsamı (CANLIDA) ✅
Yukarıdaki iki bilinçli boşluk kapatıldı (claim'e dokunmadan, takım istek başına DB'den cache'li çözülür).
- **#1 Board/liste satır-bazında takım kapsamı:** `TaskItemAppService.GetAllAsync` artık **sunucu tarafında** satır filtresi uyguluyor: Admin tümü; Admin olmayan yalnız `TeamId == kendi takımı` **VEYA** `TeamId == null` (takımsız/legacy) **VEYA** kendine/2. sorumlu atanan görevler. Böylece Pano controller filtresine güvenmeden izole. Mevcut 3 görev TeamId=NULL olduğundan `null` şartıyla görünür kalır (regresyon yok); yeni görevler `ResolveTeamIdForNewTaskAsync` ile takım alır. (Not: `TaskItem.IsManagerForTask` zaten TakımLideri'ni kendi takımına sınırlıyordu — görev *işlemi* tarafı zaten doğruydu; eksik olan satır *görünürlüğüydü*.)
- **#2 ActivitySubject yönetici işlem kapsamı:** yeni `IsManagerForSubject` (Admin=tümü, TakımLideri=kendi takımı/takımsız) → `EnsureCanManage`, `LogEffortAsync`, `DeleteEffortAsync` ve `MapSubject.CanManage/CanLogEffort` artık takım-duyarlı. TakımLideri artık başka takımın faaliyet konusunu id ile yönetemiyor/efor giremiyor/silemiyor.
- **Doğrulama:** Build 0/0, HTTP 302, error log boş. Yedek `backup2/20260723_134248_scopefix/`.
- **[KAPANDI] Null-takım backfill:** Legacy null-takım kayıtlar atanan kişinin takımına taşındı (kullanıcı onayıyla, `dbq.ps1` UPDATE): **3 görev + 2 faaliyet konusu → TeamId=1 (Infrastructure Team)**. Doğrulandı: null-takım kayıt kalmadı. `GetAllAsync`'teki `TeamId == null` şartı artık uykuda bir güvenlik ağı (atanmamış-takım edge-case için korunuyor; kaldırılmadı). Yeni kayıtlar zaten takım alıyor.

## Görev tarih+saat, takvim düzeltmeleri, kişi kartı & proje detay UX (CANLIDA) ✅
- **Görev başlangıcı tarih+saat:** `Tasks/Create` ve `Tasks/Edit`'te Başlangıç alanı `type="datetime-local"`. Create GET varsayılanı `DateTime.Now` (o anki saat otomatik gelir). `StartDate`/`DueDate` zaten `DateTime?` — şema değişmedi. **Bonus bug:** Edit'te StartDate alanı yoktu → her düzenlemede `ObjectMapper.Map` StartDate'i null'lıyordu; alan eklenince düzeldi.
- **Takvim saat gösterimi:** Kişi kartı haftalık takvim + `Employees/Calendar` görevleri artık `startDate` (saatliyse) ile çiziliyor; saat 00:00 ise `allDay` (üst şerit) — "hep üstte 00.00" sorunu bitti. `GetCalendarTasksAsync` sorgusu StartDate VEYA DueDate ay içindeyse getirecek şekilde genişletildi.
- **Takvim view-switch bug'ı:** Ay/Hafta/Liste'ye geçince görevler kayboluyordu — sebep Card takviminin `GetCalendarTasks`'e `info.start`'tan ay/yıl göndermesiydi (ay görünümünde `info.start` önceki aya kayıp yanlış ay çekiliyordu). Card takvimi artık `TaskItem/GetAll` ile tüm görevleri tek seferde çekiyor (ay bağımlılığı yok) → tüm görünümlerde çalışıyor.
- **Kişi kartı "Tamamlanan" sadece sayı:** Atanan Görevler altında tamamlananlar tam liste yerine **rozet+sayı** (Görev Sorgula'ya link) — ileride liste şişip yandaki 7/30 günlük tabloyu aşağı kaydırmıyor.
- **Proje detay "Görev Ekle" butonu:** Görevler kartı başlığına (Faaliyet Ekle ile simetrik) `/Tasks/Create?projectId=` butonu — kategori+sorumlular projeden dolu gelir.
- **Doğrulama:** Build 0/0, HTTP 302, error log boş. Yedek `backup2/20260723_140109_datetime_cal/`.

## Proje 1./2. sorumlu = takım üyesi + çift "Görev Ekle" temizliği (CANLIDA) ✅
- **Sorun:** Projede 1./2. sorumlu seçilince "Takım 0 üye" görünüyordu (sorumlular `Project.PrimaryResponsibleId/SecondaryResponsibleId`'ye yazılıyor ama `ProjectEmployee` üyesi oluşturulmuyordu).
- **Çözüm:** `ProjectAppService.SyncResponsibleMembersAsync` — Create/Update'te 1. Sorumlu (rol "1. Sorumlu", IsManager=true) ve 2. Sorumlu (rol "2. Sorumlu") **ProjectEmployee olarak upsert** edilir; sorumlu değişirse eski etiketli üye "Üye"ye çevrilir (üyelikten atılmaz). Böylece "Takım (N üye)" doğru sayıyı gösterir.
- **Mevcut veri backfill:** 3 mevcut projeye sorumlular üye olarak eklendi (`dbq.ps1`): 3 birincil + 2 ikincil = 5 üye. Doğrulandı (Proje 1→2, 2→1, 3→2 üye).
- **Çift buton temizliği:** Proje detay üstündeki eski "Görev Ekle" butonu kaldırıldı; artık yalnız Görevler kartı başlığındaki buton var (Düzenle üstte kaldı).
- **Doğrulama:** Build 0/0, HTTP 302, error log boş. Yedek `backup2/20260723_140734_projmembers/`.

## Proje kodu otomatik artan + proje listeleme düzeltmeleri (CANLIDA) ✅
- **Otomatik proje kodu:** `ProjectAppService.GetNextCodeAsync()` — mevcut `PRJ-###` kodlarındaki en büyük numaradan +1 (3 hane). Create GET formu kodu ön-doldurur ve alan **salt-okunur** (Create+Edit); `CreateAsync` kod boşsa otomatik atar. Manuel giriş kalktı → eskiden oluşan **PRJ-001 duplikeleri** artık olmayacak. (Sonraki kod: PRJ-004.)
- **Proje listeleme (2 kök neden):**
  1. Dashboard "Aktif Projeler" yalnız `Status==Devam` alıyordu → "Planlandı" projeler (örn. Proje 2) hiç görünmüyordu. Artık **tamamlanmamış+iptal olmayan** (Planlandı+Devam) açık projeleri gösteriyor (Take 6, Devam önce).
  2. Tüm projelerin `TeamId`'si NULL'dı → non-admin dashboard filtresi (`p.TeamId==myTeam`) hepsini eliyordu (non-admin 0 proje görürdü). Düzeltmeler: (a) filtreye `|| TeamId==null` toleransı, (b) `CreateAsync` yeni projeye 1. Sorumlu'nun takımını atar, (c) mevcut 4 proje `TeamId=1`'e backfill edildi (`dbq.ps1`).
- **Doğrulama:** Build 0/0, HTTP 302, error log boş; tüm projeler TeamId=1, sonraki kod PRJ-004. Yedek `backup2/20260723_143325_projcode/`.

## gelistirme-talepleri-v4.md — 7 bölümün TAMAMI (CANLIDA) ✅
3 batch halinde build+deploy edildi (2 migration: V4_PriorityScore_ActivityTypes, V4_ThemeSettings).

- **P-A Görev/Deadline/Önem:** `TaskItem.PriorityScore` (1-10) eklendi (migration); Create/Edit formunda 1-10 seçici, listeler/pano/takvim `PriorityScore` desc sıralı (`PriorityFromScore` renk enum senkronu). Yeni görevde Deadline varsayılan **yarın** (`DateTime.Now.AddDays(1)`). Süre (EstimatedHours) zorunluluğu **kaldırıldı** (0 kabul, min=0).
- **P-B Yetki/Kategori:** Uzman **proje oluşturamaz** (ProjectsController Create GET/POST + `ProjectAppService.CreateAsync` guard + Index buton gizli). Admin **Ana Kategori CRUD** açıldı (`CategoryAppService.Create/Update/Delete` artık çalışır; `Categories.cshtml` düzenlenebilir ad + Yeni/Sil + `AdminController.SaveCategory` Id=0 create ayrımı, DeleteCategory gerçek silme).
- **P-C Faaliyet Tipleri:** Yeni `ActivityTypeDef` entity (tablo `ActivityTypes`, migration+seed 12 tip). `IActivityTypeAppService` CRUD (Admin, manuel claim). `Admin/ActivityTypes.cshtml` ekranı + Admin/Index kartı. Faaliyet efor formu tipleri **dinamik** listeden çekiyor.
- **P-D Proje sekmeleri:** `Projects/Index`'e **Tüm Projeler / Projelerim** (1./2. sorumlu, client-side filtre, EmployeeId claim ile).
- **P-E Raporlama/Sorgu:** `GetTasksInput.ActivityType` + Görev Sorgula'ya **Görev Tipi** filtresi (+Excel route). Kişisel rapor: `ActivityTypeBreakdown` (efor tipi kırılımı) [zaten eklenmişti] + **"Ne Yaptı?" detaylı faaliyet tablosu** (`DetailedActivities`: Tarih/Alt Başlık/Tip/Detay Not/Süre; ActivitySubject include).
- **P-F 8 saat otomasyonu:** `ActivitySubjectAppService.CompleteDayTo8HoursAsync` — kullanıcının `SubCategoryResponsibility`'lerinden 1'er saatlik "Rutin Kontrol" ActivityLog üretir, gün 8 saate tamamlanır. `Activities/Index`'te "Günü 8 Saate Tamamla" butonu + `ActivitiesController.CompleteDayTo8Hours`.
- **P-G UI/Tema/Logo/Foto:** Yeni `ThemeSettings` entity (PrimaryColor hex + LogoUrl + BrandName, migration). `IThemeSettingsAppService` (oku herkes, yaz Admin). `Admin/Theme.cshtml` (renk seçici + marka + logo upload) + Admin/Index kartı. `_Layout` `@inject` ile temayı okuyup **dinamik CSS** (`--app-primary` + bg/btn/text-primary override) ve **logo/marka** enjekte ediyor. Profil fotoğrafı: `Employees/Create+Edit` multipart + `photoFile` upload (`wwwroot/uploads/photos`), PhotoUrl hidden round-trip.
- **Doğrulama:** Her batch build 0/0; migrationlar uygulandı (PriorityScore kolonu, ActivityTypes 12 kayıt, ThemeSettings tablo); HTTP 302 + Login 200 (layout tema inject OK); error log boş. Yedekler `backup2/…_v4batch1|E|FG`.
- **Not (yorum):** P-F "otomatik" tek-tık öneri olarak uygulandı (arka plan cron değil) — kullanıcı onayıyla efor üretir; istenirse zamanlanmış servise çevrilebilir.

## Tema uygulanmama bug fix + tip bazlı birleşik rapor (CANLIDA) ✅
- **Bug:** Admin tema (renk #677489 + logo) kaydediliyordu ama siteye yansımıyordu. Kök neden: `_Layout` `@inject IThemeSettingsAppService` + `await GetAsync()` çağrısında **aktif UnitOfWork yoktu** (`ArgumentNullException: unitOfWork` — layout render'ında request UoW kapanmış), try/catch yutup varsayılan #0d6efd'e düşüyordu.
- **Çözüm:** Tema `ActivityManagementControllerBase.OnActionExecutionAsync` içinde **`UnitOfWorkManager.Begin()` ile açık UoW** açılarak yükleniyor ve `ViewData["ThemePrimary/ThemeLogo/ThemeBrand"]`'e konuyor; `_Layout` `@inject` yerine ViewData'dan okuyor. Doğrulandı: kimlik doğrulamalı sayfada `--app-primary = #677489`, logo navbar'da. (RequestServices ile çözülen AppService UoW-intercept edilmediğinden açık UoW şart.)
- **Tip bazlı birleşik rapor:** Kişisel raporda `CombinedTypeBreakdown` — her tip için **görev (tamamlanan) + faaliyet (efor)** adedi ve toplam saat bir arada; özet metni "3 Bakım görevi ve 5 Bakım faaliyeti; ..." biçiminde. `ReportAppService` görev `ActivityType` enum'unu + `ActivityLog.ActivityType` (dinamik) string'ini tip adına göre birleştiriyor. `PersonalReport.cshtml`'e "Tip Bazlı Dağılım (Görev + Faaliyet)" tablosu eklendi.
- **Doğrulama:** Build 0/0, HTTP 302, tema render doğrulandı, error log temiz. Yedek `backup2/…_themefix*`.

## R1: Günlük Efor sayfası + R2: Rol × Sayfa erişim matrisi (CANLIDA) ✅

### R1 — Günlük Efor ("Efor 8 Saat Tamamla")
- Yeni sayfa `Activities/Today` (menüde **Günlük Efor**): seçili günün (varsayılan bugün, üstte tarih seçici) efor kayıtları + o güne ait görevler; toplam efor + **eksik efor uyarısı** (8 saate göre "X saat eksik efor").
- **"Efor 8 Saat Tamamla"** butonu: eksik saati, kullanıcının **yalnız 1. SORUMLU (Primary)** olduğu alt kategori matrislerinden 1'er saatlik "Rutin Kontrol" ActivityLog'u ile doldurur (`CompleteDayTo8HoursAsync` Primary filtresi eklendi).
- **Efor ekle/düzenle/sil**: manuel efor ekleme (`AddManualEffortAsync`), düzenleme (`UpdateEffortAsync` — otomatik girilenler dahil), silme. Faaliyet tipi dinamik listeden.
- Activities/Index'teki eski "Günü 8 Saate Tamamla" butonu → "Günlük Efor" sayfasına link.

### R2 — Rol × Sayfa Erişim Matrisi (dinamik yetki)
- **Model:** `AppRoleDef` (sistem+özel roller) + `RolePageAccess` (rol×sayfa) entity'leri + `Core/Authorization/PageCatalog` (11 sayfa + varsayılan erişim + sistem rolleri). Migration `V4_RolePageAccess`; seed idempotent (3 sistem rolü + mevcut yetkiler: Admin 11/11, TakımLideri 10/11 [Görevler-admin listesi hariç], Uzman 9/11).
- **Servis:** `IAccessControlAppService` — `GetAllowedPagesAsync(role)` (Admin=tümü; kayıt yoksa sistem rolü varsayılanı; özel rol boş), matris oku/yaz (Admin), rol CRUD (sistem rolü/atanmış rol silinemez).
- **Enforcement:** `ActivityManagementControllerBase.OnActionExecutionAsync` **açık UoW** içinde tema + izinli sayfaları ViewData'ya koyar; `CanAccessPage`/`EnsurePageAccess` helper. Giriş action'larına guard (Projects/Tasks/Board/MyTasks/TaskQuery/Activities/DailyEffort/Employees/Reports/Admin). **Admin her zaman tam erişim** (kilitlenme yok); set yüklenemezse fail-open. Menü (`_Layout`) `Can(key)` ile linkleri gizler.
- **UI:** `Admin/RoleAccess` matris ekranı (checkbox rol×sayfa, Admin salt-okunur; yeni rol ekle/sil) + Admin/Index kartı. Roller ekranı rol dropdown'u artık `AppRoleDef`'ten (özel roller atanabilir).
- **Doğrulama:** Build 0/0. Migration uygulandı, seed doğru (rol başına izin sayıları). Admin login → `/ /Admin /Admin/RoleAccess /Projects /Activities/Today` hepsi **200** (kilitlenme yok). Yedekler `backup2/…_r1_dailyeffort|_r2_roleaccess`.
- **Not:** Admin Paneli için controller'da `[Authorize(Roles="Admin,TakımLideri")]` hard gate duruyor; özel rolün Admin paneline erişmesi gerekirse bu ayrıca genişletilmeli (matris menüyü/entry'yi yönetir, bu attribute ayrı katman).

## Görev tarih/saat ayrımı + "kendine görev = yetkiniz yok" bug fix (CANLIDA) ✅
- **Tarih/saat ayrımı:** Görev Create/Edit'te tek `datetime-local` yerine **ayrı Tarih (`type=date`, varsayılan bugün) + Saat (`type=time`, hh:mm, opsiyonel)**. Controller `CombineStartDate(datePart,timePart)` ile birleştirir; **saat boşsa gün başı (00:00 = takvimde tüm gün), doluysa başlangıç saati**. Create GET varsayılanı `DateTime.Today` (saat boş).
- **"Kendine görev → yetkiniz yok" bug:** Kök neden — Create POST oluşturma sonrası `Index`'e (Görevler admin listesi) yönlendiriyordu; R2 `EnsurePageAccess("Tasks")` guard'ı Uzman'da "Tasks" izni olmadığından "yetkiniz yok" veriyordu (görev aslında oluşmuştu). Düzeltme: (a) Create POST non-admin'i **MyTasks**'a yönlendirir; Uzman'ın kendine açtığı görev onaya düştüğünden **"Görev oluşturuldu ve onaya gönderildi; takım lideriniz onaylayınca aktifleşecek"** toast'ı gösterir. (b) `Tasks.Index` non-admin→MyTasks yönlendirmesi guard'dan **öne** alındı (erişim reddi yerine yönlendirme).
- **Doğrulama:** Build 0/0, HTTP 302, Create formunda startDatePart/startTimePart var (datetime-local kalktı), error log temiz. Yedek `backup2/…_datetime_approve`.

## Günlük efor projeye bağlama + GitHub push (CANLIDA / repo güncel) ✅
- **Rutin kontrol eforu proje bazlı:** `CompleteDayTo8HoursAsync` artık öncelikle kullanıcının **1. sorumlu (PrimaryResponsible) olduğu aktif projelere** 1'er saat "Rutin Kontrol" ActivityLog'u yazar (`ProjectId` set → efor projeye sayılır); proje yoksa 1. sorumlu alt kategori fallback. Günlük Efor listesinde Konu/Görev sütunu artık **"Proje: <ad>" / "Görev:" / "Konu:"** önekiyle dolu. Manuel efor ekleme/düzenlemeye **Proje seçici** eklendi (`AddManualEffortAsync`/`UpdateEffortAsync` `projectId` parametresi).
- **GitHub push:** Repo `https://github.com/aykutayvaz91/ActivityManagementRepo` — `git init` + `.gitignore` (secret'lar/bin/obj/log hariç, `appsettings.Production.json` **commit'lenmedi**). Remote'ta projenin eski (nested `ActivityManagement/`) 5+ commit'lik geçmişi vardı; force yerine **geçmişi koruyarak** mevcut proje kök yapıda tek commit (`5bdc126`) olarak eskinin üstüne kondu → fast-forward normal push (`4a42493..5bdc126`). Bundan sonra normal `git push` yeter (force gerekmez); geriye dönük tüm versiyonlar repoda. origin URL temiz (token persist edilmedi). **PAT sohbette göründü → kullanıcının token'ı iptal/yenilemesi önerildi.**

## Notlar / Bekleyen
- DB switch talebi: başka IP'deki cluster'a yönlendirme mümkün (`Database.Migrate()` boş DB'de tabloları+seed'i kurar). Bağlantı dizesi `appsettings.Production.json` (DPAPI). Detay istenince yapılır; mevcut veri taşınmaz (backup/restore ayrı iş).
- `[AbpAuthorize]` bazı AppService'lerde (Project Update/Delete) hâlâ var; AbpPermissionGrants tablosu yok — gerekirse manuel claim auth'a çevrilebilir (şimdilik dokunulmadı).
- v2 dökümanında "Kategori detayında sorumluluk gösterimi" ayrı bir Kategori-detay sayfası olmadığından **Sorumluluk Matrisi** ekranının kategori-grupla tablosu ile karşılandı (kişi tarafı kişi kartında).
